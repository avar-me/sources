# 2026-07-22 — av-ru: maintainer-RFI dial-eval #9 (31 замечание)

Canvas: `av-ru-maintainer-rfi-9-review`. Только `av-ru`. 22 статьи.

## Отклонено (FP)

- `багь-багьараб`: 'рагь-рагьарал' ≠ headword [отклонено: классное согласование]
- `багьизе`: 'рагьана' ≠ 'багьана' [отклонено: классное согласование]
- `берзул`: 'лобзание глаз' [отклонено: словарная поэтика]
- `дал`: 'даль' → 'дал'? [отклонено: «даль» — норма]
- `ластикалъул`: русский корректен, неточен только англ. перевод [отклонено: av-en не трогаем]
- `мицӏир` (forms[3]): 'моцӏрол' [отклонено: форма 'мицӏир' корректна]
- `надзирательлъи` (s[0].text): избыточно, но допустимо [отклонено: норма]
- `ноцӏмахӏаб` (s[0].text): 'похожий на сучка' [отклонено: `ноцӏ` = сук/сучок; `-махӏаб` = -подобный]
- `паркъизе` (s[0].ex[0].ru): 'сиял'→'сиял' [отклонено: пустое замечание; текст корректен]

## Опечатки

- `ноябралъул` (s[1].ex[1].ru): «дожды» → «дожди»
- `нурдакъаб` (s[0].text): «непригдядный» → «неприглядный»
- `нуцӏил` (s[1].text): «дверьной» → «дверной»
- `нуцӏил` (s[1].ex[0].ru): «дверьная» → «дверная»
- `окоп` (s[0].ex[0].ru): «отрить» → «отрыть»
- `ортодоксалияб` (s[0].text): «ортодокссальный» → «ортодоксальный»
- `отоларингология` (s[0].text): «отоларинтология» → «отоларингология»
- `отоларингологиялъул` (s[1].text): «отоларинтологический» → «отоларингологический»
- `палатаялъул` (s[1].ex[0].av): «палатаялъулялъул» → «палатаялъул»
- `палугьанлъи` (s[0].ex[0].ru): «канатоходстом» → «канатоходством»
- `паровоз` (s[0].ex[0].ru): «звод» → «завод»

## Структура

- `нурагъине`: POS сущ. → глагол/инфинитив
- `нус` (нож, hom.1): POS глагол → существительное/именительный
- `нус` (hom.3): text «краткая форма от» → `comment: краткая форма от 'нусго'` + `see_also from`
- `нух` (раз, hom.2): POS глагол → существительное/именительный
- `нуцалчи`: POS глагол → существительное/именительный (senses пустые)
- `оппозиционер`, `оппонент`: POS прил. → существительное/именительный
- `оппозицияб`: POS сущ. → прилагательное
- `ортопедлъи`: POS послелог → существительное/именительный
- `папуасав`: form мн.ч. → именительный
- `оперрета` → `оперетта` (word + forms + text)
- `острит` → `остит` (word + forms + text; мед. «ракьа унти»)
