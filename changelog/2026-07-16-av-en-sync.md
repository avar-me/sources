# 2026-07-16 — av-en: синхронизация с av-ru (RFI буквы А + чистка «выражение»)

Обновлённый `av-en.jsonl` от мейнтейнера: перенесены сегодняшние правки из
`av-ru.jsonl` (два коммита — maintainer-RFI по букве А и чистка паразитной
пометы «выражение»). Соответствие av-ru ↔ av-en:

## Чистка «выражение» → idiom

- Снята паразитная метка `idiom` (= «выражение») из `labels`: 1031 sense.
- Восстановлен `pos` вместо `idiom`: 840 → `noun`, 37 → `verb`, 4 → `adverb`.
  Фразовые устойчивые выражения сохранили `pos: "idiom"`.

## Maintainer-RFI (буква А)

- POS/форма: `verb → noun` (3), `noun → adjective` (2: `авалияб`, `аскарияб`),
  `noun → verb` (1: `азине`), `numeral → adjective` (1: `азербайжан`),
  `interjection → noun` (1: `ах`), `adjective → noun` (1: `асхӏаб`),
  `pronoun → verb` (1: `ана`), `verb → pronoun` (1: `айго`).
- Глоссы/тексты (157 sense): `абад` → eternity, `ах` → garden; orchard,
  `абго` → that's it; that is all, и переносы опечаток/грам-помет из RFI.
- Комментарии (45) и примеры (109): точечные правки, `comment_lang: "av"`
  сохранён там, где аварский comment оставлен намеренно.
- Слит дубль `анлънусабго` (2 → 1 статья, «all six hundred»).

Файл прислан мейнтейнером готовым; проверено на валидность JSONL и сверено
пословно с изменениями `av-ru.jsonl` за 2026-07-16.
