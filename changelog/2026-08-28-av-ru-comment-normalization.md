# 2026-08-28 — av-ru: нормализация `comment` и `comment_lang`

По canvas `av-ru-literal-comments-review` одобрено 88 из 90 предложений: 27 голых маркеров `букв.`, 33 случая с неверным положением маркера и 28 аварских `see_also[].comment`. Предложение B13 (`буголъизе`) отклонено и не применялось. Предложение B14 (`вагӏалайкумсалам`) в форме из canvas отклонено, после чего пример исправлен по отдельной формулировке мейнтейнера.

## Дополнительные уточнения после canvas

- `бессизе`, `senses[0].examples[2].ru` (омоним 2): «в душу проникнута любовь» → «в душу проникла любовь».
- `бетӏергьан`, `senses[0].examples[11].av`: `бетӏергьаасул` → `бетӏергьанасул`.
- `борохь`, `senses[0].examples[2].comment`: «букв. словно» → «букв. словно — 'гӏадин'».
- `вагӏалайкумсалам`, `senses[0].examples[0]`: `ru` «и тебе мир» → «приветствие»; `comment` «букв.» → «букв. тебе мир».

## B01 — `азине` · `senses[0].examples[1]`

Сейчас:

```json
{"ru": "и ты похудел намного (десять раз) больше, чем я", "comment": "букв."}
```

Стало:

```json
{"ru": "и ты похудел намного больше, чем я", "comment": "букв. десять раз"}
```

## L01 — `ахир` · `see_also[0]`

Сейчас:

```json
{"target": "къиямат", "kind": "see", "comment": "ахир къиямасеб къо"}
```

Стало:

```json
{"target": "къиямат", "kind": "see", "comment": "ахир къиямасеб къо", "comment_lang": "av"}
```

## M01 — `бадиб` · `senses[0].examples[27]`

Сейчас:

```json
{"ru": "приставать, надоедать", "comment": "приставать с ножом к горлу букв. из глаз жир вытаскивать"}
```

Стало:

```json
{"ru": "приставать, надоедать; приставать с ножом к горлу", "comment": "букв. из глаз жир вытаскивать"}
```

## B02 — `базе` · `senses[16].examples[0]`

Сейчас:

```json
{"ru": "дать (сыпать) корм скоту", "comment": "букв."}
```

Стало:

```json
{"ru": "дать корм скоту", "comment": "букв. сыпать"}
```

## B03 — `базе` · `senses[28].examples[0]`

Сейчас:

```json
{"ru": "давать (посыпать) взаймы мерку пшеницы", "comment": "букв."}
```

Стало:

```json
{"ru": "давать взаймы мерку пшеницы", "comment": "букв. посыпать"}
```

## L02 — `бакӏ` · `see_also[0]`

Сейчас:

```json
{"target": "бакӏитӏакӏус", "kind": "see", "comment": "бакӏитӏе рахъин"}
```

Стало:

```json
{"target": "бакӏитӏакӏус", "kind": "see", "comment": "бакӏитӏе рахъин", "comment_lang": "av"}
```

## L03 — `бахунеб-толеб` · `see_also[0]`

Сейчас:

```json
{"target": "баха-толеб жо", "kind": "see", "comment": "бахунеб-толеб жо"}
```

Стало:

```json
{"target": "баха-толеб жо", "kind": "see", "comment": "бахунеб-толеб жо", "comment_lang": "av"}
```

## B04 — `бахъизе` · `senses[2].examples[3]`

Сейчас:

```json
{"ru": "освободить (вытащить) из тюрьмы", "comment": "букв."}
```

Стало:

```json
{"ru": "освободить из тюрьмы", "comment": "букв. вытащить"}
```

## L04 — `башла` · `see_also[0]`

Сейчас:

```json
{"target": "байбихьизе", "kind": "see", "comment": "башла гьабизе"}
```

Стало:

```json
{"target": "байбихьизе", "kind": "see", "comment": "башла гьабизе", "comment_lang": "av"}
```

## L05 — `бегӏукъ` · `see_also[0]`

Сейчас:

```json
{"target": "бегӏукълъизабизе", "kind": "see", "comment": "бегӏукъ(го) гьабизе"}
```

Стало:

```json
{"target": "бегӏукълъизабизе", "kind": "see", "comment": "бегӏукъ(го) гьабизе", "comment_lang": "av"}
```

## M02 — `бежа-белъине` · `senses[0].examples[1]`

Сейчас:

```json
{"ru": "некачественно подготовленное; недожаренное и недоваренное", "comment": "о блюде букв."}
```

Стало:

```json
{"ru": "некачественно подготовленное", "comment": "о блюде; букв. недожаренное и недоваренное"}
```

## B05 — `бесдал` · `senses[0].examples[6]`

Сейчас:

```json
{"ru": "да утонет в помоях мачеха, да катается (утонет) в масле родная мать", "comment": "букв."}
```

Стало:

```json
{"ru": "да утонет в помоях мачеха, да катается в масле родная мать", "comment": "букв. утонет"}
```

## B06 — `бесдаллъи` · `senses[1].examples[0]`

Сейчас:

```json
{"ru": "не дай бог никому испытать (увидеть) сиротство", "comment": "букв."}
```

Стало:

```json
{"ru": "не дай бог никому испытать сиротство", "comment": "букв. увидеть"}
```

## B07 — `бессизе` · `senses[0].examples[2]`

Сейчас:

```json
{"ru": "в душу (в сердце) проникнута любовь", "comment": "букв."}
```

Стало:

```json
{"ru": "в душу проникнута любовь", "comment": "букв. в сердце"}
```

## B08 — `бетӏер` · `senses[0].examples[44]`

Сейчас:

```json
{"ru": "каша (мякина) в голове", "comment": "букв."}
```

Стало:

```json
{"ru": "каша в голове", "comment": "букв. мякина"}
```

## B09 — `бетӏер` · `senses[0].examples[90]`

Сейчас:

```json
{"ru": "лезть (прыгать) на голову", "comment": "букв."}
```

Стало:

```json
{"ru": "лезть на голову", "comment": "букв. прыгать"}
```

## B10 — `бетӏергьан` · `senses[0].examples[11]`

Сейчас:

```json
{"ru": "если у работодателя нутро (живот) не болит, у работников спина не будет болеть", "comment": "букв."}
```

Стало:

```json
{"ru": "если у работодателя нутро не болит, у работников спина не будет болеть", "comment": "букв. живот"}
```

## L06 — `бихьинаб` · `see_also[0]`

Сейчас:

```json
{"target": "куяб", "kind": "see", "comment": "бихьинаб кӏерт"}
```

Стало:

```json
{"target": "куяб", "kind": "see", "comment": "бихьинаб кӏерт", "comment_lang": "av"}
```

## B11 — `борохь` · `senses[0].examples[2]`

Сейчас:

```json
{"ru": "бесшумно, тихо как (словно) змея", "comment": "букв."}
```

Стало:

```json
{"ru": "бесшумно, тихо как змея", "comment": "букв. словно"}
```

## B12 — `борохь` · `senses[0].examples[4]`

Сейчас:

```json
{"ru": "непостоянно, обманчиво (меняется), как змея сбрасывает шкуру", "comment": "букв."}
```

Стало:

```json
{"ru": "непостоянно, обманчиво, как змея сбрасывает шкуру", "comment": "букв. меняется"}
```

## M03 — `габур` · `senses[0].examples[33]`

Сейчас:

```json
{"ru": "сажать, посадить на шею; вешать на шею", "comment": "кого-либо букв."}
```

Стало:

```json
{"ru": "сажать, посадить на шею", "comment": "кого-либо; букв. вешать на шею"}
```

## L07 — `гамагъ` · `see_also[0]`

Сейчас:

```json
{"target": "гьанта", "kind": "see", "comment": "гамагъ битӏун буго"}
```

Стало:

```json
{"target": "гьанта", "kind": "see", "comment": "гамагъ битӏун буго", "comment_lang": "av"}
```

## B15 — `гебегизе` · `senses[0].examples[3]`

Сейчас:

```json
{"ru": "дети столкнули (пустили катиться) камни вниз", "comment": "букв."}
```

Стало:

```json
{"ru": "дети столкнули камни вниз", "comment": "букв. пустили катиться"}
```

## B16 — `гьан` · `senses[0].examples[13]`

Сейчас:

```json
{"ru": "постное (худое) мясо", "comment": "букв."}
```

Стало:

```json
{"ru": "постное мясо", "comment": "букв. худое"}
```

## B17 — `гьан` · `senses[0].examples[14]`

Сейчас:

```json
{"ru": "сушёное (старое) мясо", "comment": "букв."}
```

Стало:

```json
{"ru": "сушёное мясо", "comment": "букв. старое"}
```

## B18 — `гьан` · `senses[0].examples[16]`

Сейчас:

```json
{"ru": "вяленое (сушёное) мясо", "comment": "букв."}
```

Стало:

```json
{"ru": "вяленое мясо", "comment": "букв. сушёное"}
```

## B19 — `гьаракь` · `senses[1].examples[0]`

Сейчас:

```json
{"ru": "приятный (красивый) голос", "comment": "букв."}
```

Стало:

```json
{"ru": "приятный голос", "comment": "букв. красивый"}
```

## M04 — `гьесадизе` · `senses[1].examples[1]`

Сейчас:

```json
{"ru": "грозить в лицо", "comment": "кому-либо букв.; в глаза"}
```

Стало:

```json
{"ru": "грозить в лицо", "comment": "кому-либо; букв. в глаза"}
```

## M05 — `гьой` · `senses[0].examples[19]`

Сейчас:

```json
{"ru": "обильно кидать куда попало; кидать как камни на собаку", "comment": "что-либо букв."}
```

Стало:

```json
{"ru": "обильно кидать куда попало", "comment": "что-либо; букв. кидать как камни на собаку"}
```

## B20 — `гьоло` · `senses[0].examples[1]`

Сейчас:

```json
{"ru": "собирать (рвать) бобы", "comment": "букв."}
```

Стало:

```json
{"ru": "собирать бобы", "comment": "букв. рвать"}
```

## M06 — `гьоцӏо` · `senses[0].examples[9]`

Сейчас:

```json
{"ru": "от него", "comment": "мало пользы букв.; он не соберёт мёда"}
```

Стало:

```json
{"ru": "от него мало пользы", "comment": "букв. он не соберёт мёда"}
```

## M07 — `гьоцӏо` · `senses[0].examples[13]`

Сейчас:

```json
{"ru": "испортить; в мёд соль сыпать", "comment": "что-либо букв."}
```

Стало:

```json
{"ru": "испортить", "comment": "что-либо; букв. в мёд соль сыпать"}
```

## B21 — `гӏарщ` · `senses[0].examples[1]`

Сейчас:

```json
{"ru": "длинный (как верёвка) язык", "comment": "букв."}
```

Стало:

```json
{"ru": "длинный язык", "comment": "букв. как верёвка"}
```

## B22 — `гӏела` · `senses[0].examples[0]`

Сейчас:

```json
{"ru": "довольно, голова болит (не суши голову)!", "comment": "букв."}
```

Стало:

```json
{"ru": "довольно, голова болит!", "comment": "букв. не суши голову"}
```

## M08 — `гӏин` · `senses[0].examples[22]`

Сейчас:

```json
{"ru": "прочитать вслух; влить в ухо", "comment": "напр. Коран букв."}
```

Стало:

```json
{"ru": "прочитать вслух", "comment": "напр. Коран; букв. влить в ухо"}
```

## M09 — `дахӏа` · `senses[1].examples[0]`

Сейчас:

```json
{"ru": "(из)баловать; стоять, держа цветок в руке", "comment": "напр. ребёнка букв."}
```

Стало:

```json
{"ru": "(из)баловать", "comment": "напр. ребёнка; букв. стоять, держа цветок в руке"}
```

## M10 — `доба` · `senses[0].examples[10]`

Сейчас:

```json
{"ru": "зря ходил (или ездил); туда и обратно дорога оказалась равной", "comment": "куда-либо букв."}
```

Стало:

```json
{"ru": "зря ходил (или ездил)", "comment": "куда-либо; букв. туда и обратно дорога оказалась равной"}
```

## L08 — `захӏанкӏун` · `see_also[0]`

Сейчас:

```json
{"target": "занкӏун", "kind": "see", "comment": "захӏанкӏун захӏанкӏун цӏезе"}
```

Стало:

```json
{"target": "занкӏун", "kind": "see", "comment": "захӏанкӏун захӏанкӏун цӏезе", "comment_lang": "av"}
```

## L09 — `исламияб` · `see_also[0]`

Сейчас:

```json
{"target": "ислам", "kind": "see", "comment": "дин"}
```

Стало:

```json
{"target": "ислам", "kind": "see", "comment": "дин", "comment_lang": "av"}
```

## M11 — `керен` · `senses[2].examples[4]`

Сейчас:

```json
{"ru": "влюбиться в; из груди сердце вырвать", "comment": "кого-либо букв."}
```

Стало:

```json
{"ru": "влюбиться в", "comment": "кого-либо; букв. из груди сердце вырвать"}
```

## M12 — `къварилъи` · `senses[3].examples[3]`

Сейчас:

```json
{"ru": "излить душу; горе облегчить", "comment": "кому-либо букв."}
```

Стало:

```json
{"ru": "излить душу", "comment": "кому-либо; букв. горе облегчить"}
```

## M13 — `къватӏиб` · `senses[0].examples[20]`

Сейчас:

```json
{"ru": "выгонять, выгнать; указать на дверь; выпроводить за дверь; наружу гонять", "comment": "кому-либо букв."}
```

Стало:

```json
{"ru": "выгонять, выгнать; указать на дверь; выпроводить за дверь", "comment": "кому-либо; букв. наружу гонять"}
```

## M14 — `къвачӏа` · `senses[0].examples[8]`

Сейчас:

```json
{"ru": "уходить, уйти", "comment": "сматывать удочки букв. мешок тащить"}
```

Стало:

```json
{"ru": "уходить, уйти; сматывать удочки", "comment": "букв. мешок тащить"}
```

## M15 — `къвачӏа` · `senses[0].examples[12]`

Сейчас:

```json
{"ru": "с длинным языком; любящий поболтать; разглашающий всё", "comment": "что надо и не надо букв.; не завязывающий горловину мешка"}
```

Стало:

```json
{"ru": "с длинным языком; любящий поболтать; разглашающий всё, что надо и не надо", "comment": "букв. не завязывающий горловину мешка"}
```

## M16 — `кьолокь` · `senses[0].examples[2]`

Сейчас:

```json
{"ru": "да сдохнешь ты; да снимут тебя [сдохшего] из-под седла", "comment": "о лошади, осле и т. п. букв."}
```

Стало:

```json
{"ru": "да сдохнешь ты", "comment": "о лошади, осле и т. п.; букв. да снимут тебя [сдохшего] из-под седла"}
```

## M17 — `кӏал` · `senses[0].examples[57]`

Сейчас:

```json
{"ru": "(по)говорить, (по)беседовать; соединить уста", "comment": "о двоих букв."}
```

Стало:

```json
{"ru": "(по)говорить, (по)беседовать", "comment": "о двоих; букв. соединить уста"}
```

## M18 — `кӏал` · `senses[0].examples[70]`

Сейчас:

```json
{"ru": "заткнуть рот; остановить рот", "comment": "кому-либо букв."}
```

Стало:

```json
{"ru": "заткнуть рот", "comment": "кому-либо; букв. остановить рот"}
```

## M19 — `кӏалдиб` · `senses[0].examples[17]`

Сейчас:

```json
{"ru": "слишком высокая цена; цена, которая [едва] поместилась во рту", "comment": "чего-либо букв."}
```

Стало:

```json
{"ru": "слишком высокая цена", "comment": "чего-либо; букв. цена, которая [едва] поместилась во рту"}
```

## L10 — `кӏвахӏал` · `see_also[0]`

Сейчас:

```json
{"target": "кӏвахӏаллъизабизе", "kind": "see", "comment": "кӏвахӏал(го) гьабизе"}
```

Стало:

```json
{"target": "кӏвахӏаллъизабизе", "kind": "see", "comment": "кӏвахӏал(го) гьабизе", "comment_lang": "av"}
```

## B23 — `кӏкӏвине` · `senses[0].examples[2]`

Сейчас:

```json
{"ru": "говорить медленно, с расстановками: говорить как тот человек, который верёвку глотает", "comment": "букв."}
```

Стало:

```json
{"ru": "говорить медленно, с расстановками", "comment": "букв. говорить как тот человек, который верёвку глотает"}
```

## L11 — `лъехьей` · `see_also[0]`

Сейчас:

```json
{"target": "лъида", "kind": "see", "comment": "лъехьей гьеб тӏадкъараб?"}
```

Стало:

```json
{"target": "лъида", "kind": "see", "comment": "лъехьей гьеб тӏадкъараб?", "comment_lang": "av"}
```

## L12 — `лъехьей` · `see_also[1]`

Сейчас:

```json
{"target": "щив", "kind": "from", "comment": "лъехьей гьеб тӏадкъараб?"}
```

Стало:

```json
{"target": "щив", "kind": "from", "comment": "лъехьей гьеб тӏадкъараб?", "comment_lang": "av"}
```

## L13 — `лъехьей` · `see_also[2]`

Сейчас:

```json
{"target": "щий", "kind": "from", "comment": "лъехьей гьеб тӏадкъараб?"}
```

Стало:

```json
{"target": "щий", "kind": "from", "comment": "лъехьей гьеб тӏадкъараб?", "comment_lang": "av"}
```

## M20 — `магӏна` · `senses[0].examples[4]`

Сейчас:

```json
{"ru": "понять, дойти до сути; взять суть", "comment": "что-либо; чего-либо букв."}
```

Стало:

```json
{"ru": "понять, дойти до сути", "comment": "что-либо; чего-либо; букв. взять суть"}
```

## B24 — `макьу` · `senses[0].examples[20]`

Сейчас:

```json
{"ru": "погрузиться в сон, крепко поспать: выдёргивать корни сна", "comment": "букв."}
```

Стало:

```json
{"ru": "погрузиться в сон, крепко поспать", "comment": "букв. выдёргивать корни сна"}
```

## B25 — `малъ` · `senses[0].examples[3]`

Сейчас:

```json
{"ru": "ничего (остриженного ногтя) не стоит", "comment": "букв."}
```

Стало:

```json
{"ru": "ничего не стоит", "comment": "букв. остриженного ногтя"}
```

## B26 — `маргъалул` · `senses[1].examples[0]`

Сейчас:

```json
{"ru": "красивое (жемчужное) тело", "comment": "букв."}
```

Стало:

```json
{"ru": "красивое тело", "comment": "букв. жемчужное"}
```

## L14 — `махӏидул` · `see_also[0]`

Сейчас:

```json
{"target": "махӏитӏегь", "kind": "see", "comment": "махӏидул тӏамах"}
```

Стало:

```json
{"target": "махӏитӏегь", "kind": "see", "comment": "махӏидул тӏамах", "comment_lang": "av"}
```

## M21 — `мугъ` · `senses[0].examples[16]`

Сейчас:

```json
{"ru": "помогать, помочь кому-либо, поддержать; быть опорой; поддержать спину рукой", "comment": "кого-либо; кому-либо букв."}
```

Стало:

```json
{"ru": "помогать, помочь кому-либо, поддержать; быть опорой", "comment": "кого-либо; кому-либо; букв. поддержать спину рукой"}
```

## M22 — `пер` · `senses[0].examples[3]`

Сейчас:

```json
{"ru": "сделанный со вкусом; пища, в которой лук и соль положены в меру", "comment": "о пище букв."}
```

Стало:

```json
{"ru": "сделанный со вкусом", "comment": "о пище; букв. пища, в которой лук и соль положены в меру"}
```

## B27 — `ражи` · `senses[1].examples[4]`

Сейчас:

```json
{"ru": "февраль месяц, в котором замерзает чесночный соус", "comment": "букв."}
```

Стало:

```json
{"ru": "февраль", "comment": "букв. месяц, в котором замерзает чесночный соус"}
```

## B28 — `ракь` · `senses[6].examples[1]`

Сейчас:

```json
{"ru": "завоевать земли: отнять земли", "comment": "букв."}
```

Стало:

```json
{"ru": "завоевать земли", "comment": "букв. отнять земли"}
```

## M23 — `рухӏ` · `senses[3].examples[3]`

Сейчас:

```json
{"ru": "любить страстно; душой таять", "comment": "кого-что-либо букв."}
```

Стало:

```json
{"ru": "любить страстно", "comment": "кого-что-либо; букв. душой таять"}
```

## M24 — `секунд` · `senses[0].examples[1]`

Сейчас:

```json
{"ru": "сделать моментально", "comment": "что-либо букв.; за одну секунду"}
```

Стало:

```json
{"ru": "сделать моментально", "comment": "что-либо; букв. за одну секунду"}
```

## M25 — `солдат` · `senses[0].examples[2]`

Сейчас:

```json
{"ru": "русские солдаты; белые солдаты", "comment": "в Кавказскую войну букв."}
```

Стало:

```json
{"ru": "русские солдаты", "comment": "в Кавказскую войну; букв. белые солдаты"}
```

## M26 — `тӏехӏ` · `senses[0].examples[2]`

Сейчас:

```json
{"ru": "намять холку; бить по паршивой голове", "comment": "кому-либо букв."}
```

Стало:

```json
{"ru": "намять холку", "comment": "кому-либо; букв. бить по паршивой голове"}
```

## M27 — `тӏил` · `senses[3].examples[0]`

Сейчас:

```json
{"ru": "уходить с работы; положить ярлыгу", "comment": "о чабане, о пастухе букв."}
```

Стало:

```json
{"ru": "уходить с работы", "comment": "о чабане, о пастухе; букв. положить ярлыгу"}
```

## M28 — `тӏом` · `senses[0].examples[2]`

Сейчас:

```json
{"ru": "очень худой, тощий; зарежешь — от шкуры пользы нет и мяса на костях не имеет", "comment": "о скоте букв."}
```

Стало:

```json
{"ru": "очень худой, тощий", "comment": "о скоте; букв. зарежешь — от шкуры пользы нет и мяса на костях не имеет"}
```

## M29 — `ургьиб` · `senses[0].examples[3]`

Сейчас:

```json
{"ru": "понять, переварить; вникнуть во; пускать в утробу", "comment": "что-либо; что-либо букв."}
```

Стало:

```json
{"ru": "понять, переварить; вникнуть во", "comment": "что-либо; что-либо; букв. пускать в утробу"}
```

## L15 — `хадуб` · `see_also[1]`

Сейчас:

```json
{"target": "хадубехъерхъи", "kind": "see", "comment": "хадуб бехъерхъи"}
```

Стало:

```json
{"target": "хадубехъерхъи", "kind": "see", "comment": "хадуб бехъерхъи", "comment_lang": "av"}
```

## L16 — `хадуб` · `see_also[2]`

Сейчас:

```json
{"target": "хадугъей", "kind": "see", "comment": "хадуб гъей"}
```

Стало:

```json
{"target": "хадугъей", "kind": "see", "comment": "хадуб гъей", "comment_lang": "av"}
```

## L17 — `хадуб` · `see_also[3]`

Сейчас:

```json
{"target": "хадугӏунтӏи", "kind": "see", "comment": "хадуб гӏунтӏи"}
```

Стало:

```json
{"target": "хадугӏунтӏи", "kind": "see", "comment": "хадуб гӏунтӏи", "comment_lang": "av"}
```

## L18 — `хадуб` · `see_also[4]`

Сейчас:

```json
{"target": "хадулъугьин", "kind": "see", "comment": "хадуб лъугьин"}
```

Стало:

```json
{"target": "хадулъугьин", "kind": "see", "comment": "хадуб лъугьин", "comment_lang": "av"}
```

## L19 — `хадуб` · `see_also[5]`

Сейчас:

```json
{"target": "хадуккей", "kind": "see", "comment": "хадуб ккей"}
```

Стало:

```json
{"target": "хадуккей", "kind": "see", "comment": "хадуб ккей", "comment_lang": "av"}
```

## L20 — `хадуб` · `see_also[6]`

Сейчас:

```json
{"target": "хадучӏей", "kind": "see", "comment": "хадуб чӏей"}
```

Стало:

```json
{"target": "хадучӏей", "kind": "see", "comment": "хадуб чӏей", "comment_lang": "av"}
```

## B29 — `халатаб` · `senses[0].examples[1]`

Сейчас:

```json
{"ru": "дальняя (длинная) дорога", "comment": "букв."}
```

Стало:

```json
{"ru": "дальняя дорога", "comment": "букв. длинная"}
```

## M30 — `хахизе` · `senses[0].examples[4]`

Сейчас:

```json
{"ru": "проклинаю!", "comment": "проклятие матери в адрес сына, когда она отрекается от него за предательство, подлость и т. п., выражающее букв. \"да будет тебе отравой молоко моей груди, которым я вскормила тебя!\""}
```

Стало:

```json
{"ru": "проклинаю!", "comment": "проклятие матери в адрес сына, когда она отрекается от него за предательство, подлость и т. п.; букв. «да будет тебе отравой молоко моей груди, которым я вскормила тебя!»"}
```

## L21 — `хич-хичизабизе` · `see_also[0]`

Сейчас:

```json
{"target": "хичизабизе", "kind": "see", "comment": "хич-хичизабизе"}
```

Стало:

```json
{"target": "хичизабизе", "kind": "see", "comment": "хич-хичизабизе", "comment_lang": "av"}
```

## M31 — `хъарахъ` · `senses[0].examples[4]`

Сейчас:

```json
{"ru": "впутаться во, запутаться в чём-либо; в кустарники попасть", "comment": "что-либо букв."}
```

Стало:

```json
{"ru": "впутаться во, запутаться в чём-либо", "comment": "что-либо; букв. в кустарники попасть"}
```

## M32 — `хьаг` · `senses[0].examples[8]`

Сейчас:

```json
{"ru": "не оставлю тебя голодной, ибо умею лудить", "comment": "(букв. зашивать) кастрюли"}
```

Стало:

```json
{"ru": "не оставлю тебя голодной, ибо умею лудить", "comment": "букв. зашивать кастрюли"}
```

## L22 — `щай` · `see_also[0]`

Сейчас:

```json
{"target": "щайгурелъул", "kind": "see", "comment": "щайин абуни"}
```

Стало:

```json
{"target": "щайгурелъул", "kind": "see", "comment": "щайин абуни", "comment_lang": "av"}
```

## L23 — `щивго` · `see_also[0]`

Сейчас:

```json
{"target": "щийго", "kind": "see", "comment": "щивго"}
```

Стало:

```json
{"target": "щийго", "kind": "see", "comment": "щивго", "comment_lang": "av"}
```

## L24 — `щивго` · `see_also[1]`

Сейчас:

```json
{"target": "щалго", "kind": "see", "comment": "щивго"}
```

Стало:

```json
{"target": "щалго", "kind": "see", "comment": "щивго", "comment_lang": "av"}
```

## L25 — `щийго` · `see_also[0]`

Сейчас:

```json
{"target": "щибго", "kind": "see", "comment": "щийго"}
```

Стало:

```json
{"target": "щибго", "kind": "see", "comment": "щийго", "comment_lang": "av"}
```

## L26 — `щийго` · `see_also[1]`

Сейчас:

```json
{"target": "щалго", "kind": "see", "comment": "щийго"}
```

Стало:

```json
{"target": "щалго", "kind": "see", "comment": "щийго", "comment_lang": "av"}
```

## L27 — `эвгияли` · `see_also[0]`

Сейчас:

```json
{"target": "эйгияли", "kind": "see", "comment": "эвгияли"}
```

Стало:

```json
{"target": "эйгияли", "kind": "see", "comment": "эвгияли", "comment_lang": "av"}
```

## L28 — `эвгияли` · `see_also[1]`

Сейчас:

```json
{"target": "элгияли", "kind": "see", "comment": "эвгияли"}
```

Стало:

```json
{"target": "элгияли", "kind": "see", "comment": "эвгияли", "comment_lang": "av"}
```

## M33 — `ярагъ` · `senses[0].examples[10]`

Сейчас:

```json
{"ru": "вооружиться; вооружить; оружием снабдить", "comment": "кого-либо букв."}
```

Стало:

```json
{"ru": "вооружиться; вооружить", "comment": "кого-либо; букв. оружием снабдить"}
```
