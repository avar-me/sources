# 2026-07-23 — av-en: синхронизация с av-ru

Источник: обновлённый `data/av-en.jsonl` (синхрон с текущим `av-ru.jsonl`).
Ключи статей 1:1 с `av-ru` (22691), строк 22848.

> Файл был принесён как правка `en-av.jsonl`, но по структуре (аварские
> headwords, поле `examples[].en`) это `av-en`. Контент перенесён в
> `data/av-en.jsonl`; `en-av.jsonl` (EN→AV) восстановлен из HEAD.

## Объём относительно предыдущего av-en

| | |
|---|---|
| изменено статей | ~1473 |
| text | ~1049 |
| examples | ~509 |
| comment | ~44 |
| pos / form | ~36 / ~32 |
| labels / see_also | ~4 / ~6 |
| добавлено | `стоматологлъи` (rename с `столматологлъи`) |
| удалено | битые дубли + `столматологлъи` (−10 к числу строк: 22858→22848) |

## Зеркало недавних правок av-ru (2026-07-23)

- `буго`: text «there is; available; present; exists», comment
  «present tense of the verb 'букӏине'», form `—`, `see_also` from `букӏине`
  (в принесённом файле comment был битый `'buk'line'` — поправлено на `'букӏине'`)
- `сокӏкӏин`: text «folding; stacking», пример «folding books»
- `хӏаж`: статейный `labels: ["dialectal"]` → see `хӏеж`
- `барантик`: comment «variety of tik, eastern fabric»

## Дубли (как в av-ru)

Оставлены правильные копии, удалены битые: `ханзайин` (noun-дубль), `хер`
(`выглядить`), `хъублъи` (`погань.;`), `хӏарам`¹ (склейка в text), `хӏули`¹
(`опереться`), `чан`² (`дычь`); плюс exact-дубли строк.

## Прочее

Крупный пласт вычитки EN-глосс/примеров и выравнивания POS/form с морфологией
av-ru (в т. ч. хвост dial-eval RFI #11–#12: `ханзайин` verb, `хӏарам` чистый
text, и т. п.).
