# 2026-07-23 — av-ru: maintainer-RFI (17 замечаний)

Источник: `av-ru-maintainer-rfi.jsonl` (category `llm-av-ru-review`).

## Исправлено

### Stub

- `хӏаж` → `хӏеж`: статейный `labels: ["диалектизм"]` (пустой указатель, как
  `рицӏ`/`зарар`)

### Удалены битые дубликаты статей

У ряда слов в файле лежали две почти одинаковые статьи (одна с ошибкой, одна
уже починенная). Оставлена правильная, битая удалена:

- `ханзайин` — копия с pos «существительное» (оставлен глагол/инфинитив)
- `хер` — копия с `выглядить` (оставлен `выглядеть`)
- `хъублъи` — копия с `погань.;` (оставлен `погань;`)
- `хӏарам`¹ — копия со склейкой `…в) выпить` в text (оставлен чистый «запрет… табу»)
- `хӏули`¹ — копия с `опереться` (оставлен `опериться`)
- `чан`² — копия с `дычь` (оставлен `дичь`)

### Удалены точные дубли строк

`акварелалъул`, `анлънусабилеб`, `борхьал`, `гарал` (×2 → ×1).

Итого: −10 строк.

## Отклонено (false positive)

- `багь-багьараб` / `багьизе` (`рагь-…`): классное согласование р-, не опечатка
- `дал` text «даль»: не трогаем (отклонено по решению)
- `мицӏир` forms `моцӏрол`: не трогаем (отклонено по решению)
- `халмаж` text «халдмаж»: не трогаем (отклонено по решению)
- `ластикалъул`: русский ок; претензия к английскому переводу
- `надзирательлъи`: сам RFI — «redundant but acceptable»
- `ноцӏмахӏаб`: суффикс `-махӏаб` = «похожий на…»; `ноцӏ` = «сук, сучок»;
  gloss «похожий на сучка» верный (не «сука»/запах)
- `паркъизе`: битый RFI (`сиял`→`сиял`); русский нормален
- `берзул` «лобзание глаз»: оставлено как в источнике (не «любование»)
