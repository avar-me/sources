# 2026-07-21 — av-ru: maintainer-RFI dial-eval #8 (18 замечаний)

Canvas: `av-ru-maintainer-rfi-8-review`. Только `av-ru`. 11 статей.

## Отклонено (FP)

- `багь-багьараб`: 'рагь-рагьарал' ≠ headword [отклонено: классное согласование]
- `багьизе`: 'рагьана' ≠ 'багьана' [отклонено: классное согласование]
- `берзул`: 'лобзание глаз' [отклонено: словарная поэтика]
- `дал`: 'даль' → 'дал'? [отклонено: «даль» — норма]
- `ластикалъул`: русский корректен, неточен только англ. перевод [отклонено: av-en не трогаем]
- `мицӏир` (forms[3]): 'моцӏрол' [отклонено: форма 'мицӏир' корректна]
- `надзирательлъи` (s[0].text): избыточная формулировка, но допустима [отклонено: норма]

## Опечатки

- `мучахине` (s[0].text): «зерныстым» → «зернистым»
- `муъминчи` (s[0].text): «уверовавшийся» → «уверовавший»
- `муъминчӏужу` (s[0].text): «уверовавшаяся» → «уверовавшая»
- `нагӏанабатад` (s[0].text): «достоин проклятый» → «достоин проклятия»
- `нахъбуссине` (s[0].text): «повернутья» → «повернуться»
- `нахъекъай` (s[1].text): «отсупление» → «отступление»
- `нахъекъай` (s[2].ex[1].ru): «бестрашный» → «бесстрашный»
- `нахъияб` (s[1].text): «последный» → «последний»
- `нахътӏамизе` (s[0].text): «окладывать» → «откладывать»
- `неца` (s[0].ex[0].ru): «невод укусил» → «овод укусил»
- `ничгьечӏолъи` (s[0].text): «бесстыжие» → «бесстыдство» (абстрактное имя на -лъи)

## Структура

- `никӏалайил`: POS «глагол/инфинитив» → «существительное/родительный» (генитив от «Николай»)
