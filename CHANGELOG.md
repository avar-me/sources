## 2026-07-22 — av-ru: maintainer-RFI (dial-eval #11, 41 замечание)

Опечатки (16), структура/POS (16); FP: багь-*, берзул, дал, ластикалъул, мицӏир,
надзирательлъи, ноцӏмахӏаб, паркъизе. `руччаби` — `pluralfor` ← 'чӏужу'; `ругьун`
глосса+POS; `рокьоб` text←пядь. 31 статья.

Подробности: [`changelog/2026-07-22-av-ru-maintainer-rfi-11.md`](changelog/2026-07-22-av-ru-maintainer-rfi-11.md).

## 2026-07-22 — av-en: вычитка и синхронизация с av-ru (~266 статей)

Обновлён `data/av-en.jsonl` (правки от мейнтейнеров av-en): ~202 глоссы,
~191 пример, ~112 комментариев. Уточнены неверные кальки (`окисел` oxide,
`палтус` halibut, `пеня` fine, `охра` ochre, `папа` Pope, `пилотка` garrison cap,
`ногъаяв`/`ногъаяй` Nogai, `ракӏ` — `lit.` из `en` в comment + слияние dash).
Rename `оперрета`→`оперетта`, `острит`→`остит` (как в av-ru). Аварская сторона
`ракӏ` совпадает с текущим `av-ru`.

## 2026-07-22 — av-ru: чистка примеров `ракӏ` (букв. / грамзаметки)

Дословные глоссы из `ru` → `comment` (`букв. …`); слияние dash-примера «о сердце»;
грамзаметки в `ru` где уместно (`успокоить кого-либо`, `рекӏел бухӏи`); уточнение
`букв. чуть ли не дало трещину`; опечатка `сллова`→`слова`. 1 статья, ~96 примеров.

Подробности: [`changelog/2026-07-22-av-ru-rak-examples.md`](changelog/2026-07-22-av-ru-rak-examples.md).

## 2026-07-22 — av-ru: maintainer-RFI (dial-eval #10, 33 замечания)

Опечатки (~24), структура/POS (3); FP: багь-*, берзул, дал, ластикалъул, мицӏир,
надзирательлъи, ноцӏмахӏаб, паркъизе. `ракӏ` ex14 — глосса + букв. в comment. 24 статьи.

Подробности: [`changelog/2026-07-22-av-ru-maintainer-rfi-10.md`](changelog/2026-07-22-av-ru-maintainer-rfi-10.md).

## 2026-07-22 — av-ru: maintainer-RFI (dial-eval #9, 31 замечание)

Опечатки (~11), структура/POS (~12); FP: багь-*, берзул, дал, ластикалъул, мицӏир,
надзирательлъи, ноцӏмахӏаб, паркъизе. Rename `оперрета`→`оперетта`, `острит`→`остит`;
`нус` stub ← 'нусго'. 22 статьи.

Подробности: [`changelog/2026-07-22-av-ru-maintainer-rfi-9.md`](changelog/2026-07-22-av-ru-maintainer-rfi-9.md).

## 2026-07-21 — av-en: вычитка и синхронизация с av-ru (~1375 статей)

Обновлён `data/av-en.jsonl` (правки от мейнтейнеров av-en): ~1048 глосс,
~705 примеров, ~85 комментариев. Уточнены неверные кальки (`нефрит` nephritis,
`дипломат` briefcase, `металлист` metalworker, `кьимагӏу` sow thistle,
`нахьахӏараб` unctuous и др.). Структурно синхронизировано с av-ru раундов 5–8:
достройки av-примеров, формы `лъамизе`/`мокьрогъветӏ`, перенос примера `кӏигъеж`,
stub «from»→comment+see_also, грамзаметки в comment, снятие ложных labels,
новые senses `махӏа`/`мугъзал`, `кьабизе`/`кьер`/`кьуризе`.
Аварская сторона идентична текущему `av-ru`.

## 2026-07-21 — av-ru: maintainer-RFI (dial-eval #8, 18 замечаний)

Опечатки (~11), структура (1: `никӏалайил` POS→сущ. род.); FP: багь-*, берзул, дал,
ластикалъул, мицӏир, надзирательлъи. 11 статей.

Подробности: [`changelog/2026-07-21-av-ru-maintainer-rfi-8.md`](changelog/2026-07-21-av-ru-maintainer-rfi-8.md).

## 2026-07-21 — av-ru: maintainer-RFI (dial-eval #7, 74 замечания)

Опечатки (~62), структура/POS (~16); FP: багь-*, берзул, дал, ластикалъул, моцӏ-месячник, мицӏир.
POS «глагол»→сущ. у пустых/склоняемых статей; ложные labels; чистка forms[];
`махӏа` — суффикс '-махӏаб' отдельным sense; `мугъзал` — новый `pluralfrom`;
`мугь`/`малълъизе` — восстановление text/comment по источнику.

Подробности: [`changelog/2026-07-21-av-ru-maintainer-rfi-7.md`](changelog/2026-07-21-av-ru-maintainer-rfi-7.md).

## 2026-07-20 — av-ru: maintainer-RFI (dial-eval #6, 28 замечаний)

Опечатки (~22), структура/POS (~7); FP: багь-*, берзул, дал.
`кьабизе` — аплодисменты отдельным sense; `кьер` — цветовые примеры на омоним «цвет»;
`кӏалдиб` — глосса + букв. в comment; POS у `кьаву` / `кьерхен` / `кьучӏ`.

Подробности: [`changelog/2026-07-20-av-ru-maintainer-rfi-6.md`](changelog/2026-07-20-av-ru-maintainer-rfi-6.md).

## 2026-07-19 — av-ru: maintainer-RFI (dial-eval #5, 24 замечания)

Опечатки (~13), структура (~7); FP: багь-*, берзул, дал.
`кинабго` / `киса*` — грамзаметки в comment; `ккезе` s14 text→comment;
`къвар-къвари` / `къвачӏа` — сборка примеров.

Подробности: [`changelog/2026-07-19-av-ru-maintainer-rfi-5.md`](changelog/2026-07-19-av-ru-maintainer-rfi-5.md).

## 2026-07-19 — av-ru: maintainer-RFI (dial-eval, 71 замечание)

Разбор открытых RFI: опечатки (~46), структура/POS (~21); FP: багь-*, берзул, дал.
Причастия/мн.ч. «от» → comment + see_also from; букв. в comment; POS у звукоподражаний;
`интентдант`/`итернационал` → `интендант`/`интернационал`.

Зеркально POS/forms в `av-en` (~23 статьи).

Подробности: [`changelog/2026-07-19-av-ru-maintainer-rfi-4.md`](changelog/2026-07-19-av-ru-maintainer-rfi-4.md).

## 2026-07-18 — av-en: вычитка (~1155 статей)

Обновлён `data/av-en.jsonl` (~790 глосс, ~730 примеров, ~100 комментариев).

- Уточнены кальки и ложные чтения (`-гӏанаб` size-of, `габани` hemp fiber,
  `гӏатӏлас` satin, `бугьудизе` hoot of owl, и др.).
- Подчищены комментарии/`lit.` и примеры под актуальный av-ru.
- При ревью откат/правка: `ахӏизе` shout (не hoot); `атлас` atlas; satin;
  `варшавпахь` melchior; `армутгени` pear; `-чи` agentive suffix.

## 2026-07-18 — av-ru: maintainer-RFI (dial-eval, ~105 замечаний)

Разбор открытых RFI (хвост Б → Д): опечатки, POS, обрубленные глоссы,
причастия «от» → comment + see_also from; FP: багь-*, берзул, дал («даль»).

Зеркально POS/структура в `av-en` (~15 статей).

Подробности: [`changelog/2026-07-18-av-ru-maintainer-rfi-3.md`](changelog/2026-07-18-av-ru-maintainer-rfi-3.md).

## 2026-07-18 — av-ru/av-en: косарь — развести нож и косца

- `гъанситонус`, `чвалтанус`: text «косарь, хозяйственный нож» (comment про
  лучину у первого сохранён).
- `ецарухъан`: «косарка, косариха»; comment «женщина» убран.
- `вецарухъан`, `харицелчи`, `рецарухъаби`: без смены смысла (косарь / косары).
- en: `kosar, household knife` / `female mower; mowerwoman`; пример вецарухъан
  (stallion→mower).

## 2026-07-18 — av-en: вычитка (~149 статей)

Обновлён `data/av-en.jsonl`. По сути:

- **Глоссы (~53):** убраны лишние синонимы; исправлены ошибки модели
  (`вецарухъан`/`ецарухъан`/`гъанситонус` grand→mower = косарь; `баси` young bull).
- **precomment (~31):** `'Kh' capital letter` → `capital letter "Х"` (и др. буквы).
- **Комментарии (~20 + ~51 в примерах):** `about`→`of`, `lit.`→`literally:`,
  сокращения вроде `male; male animal`→`male`.

## 2026-07-18 — av-en: долъул / щиб — глоссы после разбора

Уточнены EN `text`: `долъул` «his; of him» / «her; its»; `щиб` sense
переспрашивания.

## 2026-07-18 — av-ru/av-en: щиб / долъул / дойго — класс в label

- `щиб`: Ⅲ из text → label `грамматический класс Ⅲ`.
- `долъул`: «и Ⅲ его, её» → senses «его» (Ⅲ) / «её» (Ⅱ); `genitivefrom`
  доб|дой порознь; `form` → родительный; see_also на оба.
- `дойго`: голый label → `грамматический класс Ⅱ` (Ⅱ уже в comment).

En: `grammatical class I/II/III`.

## 2026-07-18 — av-en: синхрон с maintainer-RFI (Б)

Уточнены ~16 глосс/примеров под правки av-ru раунда Б (`батизе`,
`батӏи-батӏияб`, `бацӏцӏадаб`/`бацӏцӏине`, `бачибокь`, `бигине`,
`бигъине`/`бигъинабизе`, `би-рахь`, `берзукь`, `бетӏер*` и др.).

## 2026-07-18 — av-ru: maintainer-RFI (dial-eval, Б продолжение)

Разбор 31 открытого RFI: русские опечатки (~22), POS/форма (6), одна av
(`беццаб`). Отклонены 3 false positive (`багь-багьараб`, `багьизе`, `берзул`).
Зеркально POS/точечные правки в `av-en`.

Подробности: [`changelog/2026-07-18-av-ru-maintainer-rfi-2.md`](changelog/2026-07-18-av-ru-maintainer-rfi-2.md).

## 2026-07-18 — av-en: вычитка (~412 статей)

Обновлён `data/av-en.jsonl` (multilang). По сути:

- **Глоссы (`text`, ~172):** уточнены омографы и ошибки модели (`баси`/`басико`
  calf vs castrated; `басма` indigo dye; `бахиллъизе` stingy; `батӏрикъ`
  patrician; ботаника `багӏарикӏкӏ` и т.п.).
- **Примеры (`examples[].en`, ~177):** правки смысла и формулировок
  (баси/басра/багӏар…).
- **Labels (~141):** `colloquial / vernacular`→`vernacular`,
  `diminutive / affectionate`→`diminutive`, `figurative word`→`figurative`;
  `grammatical class Ⅰ|Ⅱ|Ⅲ`→`I|II|III` (латиница).
- **Комментарии (~16):** мелкие уточнения (`in parts`→`into parts`,
  dough/test, reported speech).

## 2026-07-18 — av-ru/av-en: «грамматический класс Ⅰ/Ⅱ/Ⅲ» → labels

У ~45 senses фраза `грамматический класс Ⅰ|Ⅱ|Ⅲ` (en: `grammatical class …`)
вынесена из `comment`/`text` в `labels`; хвост comment после `;` сохранён.
Убраны голые labels `грамматика` / `грамматический класс` (и en `grammar` /
`grammatical class`); при наличии номерного класса — ещё и голый `класс`/`class`.
Только senses, где фраза была в `comment`/`text`; голые labels без фразы
(`долъул`, `дойго`, `добго`, `щиб` sense с «Ⅲ при вопросе…») не трогали.

## 2026-07-18 — av-ru/av-en: грамматический класс Ⅰ/Ⅱ/Ⅲ

Пометы `1`/`11`/`111 гр. кл` и варианты `I`/`II`/`III`/`Ⅰ`… перед
«грамматический класс» приведены к норме `грамматический класс Ⅰ|Ⅱ|Ⅲ`
(римские цифры U+2160…; хвосты вроде «; о мужчине» сохранены). В `av-en` —
`grammatical class Ⅰ|Ⅱ|Ⅲ`; заодно поправлены MT-артефакты (`111 deg. C`,
`1st grade`, `11th gr. cl.`). Затронуто ~33 sense в av-ru, ~46 в av-en
(щиб*/щив*/щий*/ичӏ*/жиб… + выравнивание эб/эв/эй).

## 2026-07-18 — av-en: вычитка (~665 статей)

Обновлён `data/av-en.jsonl` (multilang). По сути:

- **Имена:** `male given name` / `Proper noun (feminine)` →
  `masculine`/`feminine proper noun` (~153); `precomment` `'A' capital letter` →
  `capital letter "A"` (у диграфов — аварская буква: `"ГӀ"`, `"ХӀ"`, …).
- **Глоссы (`text`, ~167):** убраны лишние синонимы и уточнены омографы
  (`below; downstairs`→`below`, `wife; spouse`→`wife`, `boza; mash` и т.п.).
- **Комментарии примеров (~277):** `someone; anyone`→`someone`,
  `something`→`of something`, `whom/what`→`someone or something`, сокращения
  `i.e.` и т.п.
- **Синхрон с av-ru:** у 11 тканей `comment: "ххам"` + `comment_lang: "av"`
  (было `fabric`).

## 2026-07-18 — av-ru/av-en: остатки «прописная буква» / аббр. имён

- `дибир`: `comment: "'Д' прописная буква; имя собств. муж"` → `precomment` +
  `comment: "имя собственное мужское"` (en: `'D' capital letter` / `male given name`).
- `макъсуд`: разнесён нарицательный sense («цель; желание») и имя «Максуд» с
  `precomment: "'М' прописная буква"`; убрано `example.comment: "М прописная буква"`.
- `узден`, `зиярат`: развёрнуты `имя собств. муж` / `собств. имя жен.` (en —
  `male`/`female given name`).

Примеры `кӏудияб хӏарп` / `хӏарп` с `ru: "прописная буква"` и т.п. — норма, не трогали.

## 2026-07-18 — av-ru/av-en: «прописная буква» — в precomment, не в labels

У 15 имён голый `precomment: "Х"` / label `прописная буква` приведены к норме
остальных ~142 статей: `precomment: "'Х' прописная буква"` (буква по статье),
label убран. Зеркально av-en: `"'Kh' capital letter"` и т.п.

## 2026-07-18 — av-ru: comment «ххам» + влитие «ся» в text

- `comment: "ххам"` (ткань): проставлен `comment_lang: "av"` у 11 статей
  (гъараман, драп, ластик, нейлон, парча, саржа, сатин, чесуча, шевиот, шифон,
  шолк); у бадис/габардин уже было.
- `comment: "ся"` влит в `text` (6 senses): бичӏчӏизе, гьанкӏизе, сверизе,
  тир-тидизе; пударизе — «…хвастать, хвастаться»; бортизе — «…(по)терять,
  (по)теряться». Зеркально в `av-en` (`reflexive` → формы в text).

## 2026-07-18 — av-en: вычитка переводов (~380 статей)

Обновлён `data/av-en.jsonl` (multilang). По сути:

- **Глоссы (`text`, ~140):** уточнены и разведены омографы/ошибки модели —
  `ахта` merino→gelding; `ахакьчӏикӏ` scum→lickspittle/sycophant; `ахиризе`
  mint (coinage)→top (grapevine); ботаника (`ахътерек` silver/white poplar,
  `марена` madder, `poppy` без hemp и т.п.); убраны лишние синонимы
  (`bud; kidney`→`kidney`, `around; surrounding`→`around`).
- **Примеры (`examples[].en`, ~150):** исправлены смысловые сбои перевода
  (ахада defend→leave off; ахакь shepherds→sheepdogs; ахираб last light→afterlife;
  инфинитивы с to-; призыв к взаимопомощи и др.).
- **Комментарии (~220 вместе с примерами):** `masculine proper noun` →
  `male given name` (~104); управления/уточнения — `of anything; of something`→
  `something`/`to something`, `on the spot`→`from the spot`, `by anyone`→
  `with someone`, `cloth; fabric`→`fabric`.
- **POS/form** у ряда статей выровнены с морфологией (базине, бай, бакътӏерхьул…).
- **Синхрон с av-ru:** `багӏарикӏкӏбагӏаррикӏкӏ` → `багӏарикӏкӏ` (forms оба
  варианта); добавлен указатель `рицӏ` → `рицӏцӏ`.

## 2026-07-18 — av-ru: барантик — comment

comment: «разновидность тика» → «разновидность тика, восточная ткань».

## 2026-07-18 — av-ru: maintainer-RFI (dial-eval, буква А–Б)

Разбор 41 открытого RFI: русские опечатки (~21), POS/форма (8), структура
и смысл (склейка `багӏарикӏкӏ`, `багӏасдо` дягиль/дудник, `бакътӏерхьуда`
«на Западе», `базарган` формы, `бакъва-гадай` масдар и др.). 4 false positive
(классное согласование р-, круговой text у заимствования).

Подробности: [`changelog/2026-07-18-av-ru-maintainer-rfi.md`](changelog/2026-07-18-av-ru-maintainer-rfi.md).

## 2026-07-17 — av-ru: диалектные заглушки — label «диалектизм»

Stub-статьям-указателям проставлен статейный `labels: ["диалектизм"]`
(как у `абахъ` и др.): `гӏурхъи` → `гӏорхъи`, `зарар` → `зарал`, `рицӏ` → `рицӏцӏ`,
`баракат` → `баркат`, `дунял` → `дуниял`. Диалектные варианты написания. 5 статей.

## 2026-07-17 — av-ru: рицӏ — вариант написания (ива)

Заведена статья-указатель `рицӏ` → `рицӏцӏ` (`see_also` kind `see`, по образцу
`зарар`/`зарал`). `рицӏ` встречается в справочнике растений (`avar_plants.pdf`)
как вариант написания уже имеющегося `рицӏцӏ` «ива, тальник, ракита»; отдельной
глоссы не даём. 1 статья.

## 2026-07-15 — av-ru/av-en: разнести склейки `2)` и убрать сиротские цифры

- `редакторлъи`, `тӏадрекъей` — склеенные senses с `2)` разбиты.
- `хӏосил` — два sense: род. от `хӏос` / осевой; у `моцӏрол`² убран `text: "2)"`.
- `бербазе` — `сглазить` + comment `кого-либо`; у `берккезе`/`берккей` хвост `1`.
- `жал`¹ — `грива` (в av-en «hryvnia» → mane).
- У нац./прил. (`японский; 2.` и т.п.) и `социализмалъул` убраны хвосты `2.`.
- Сироты `text: "1"`/`"2"` (масдары, падежи, `мурад`, `кӏобокӏизе`…) — text удалён,
  пустые senses убраны. Зеркально в `av-en`.

## 2026-07-15 — гӏараш: порядок ключей повилика (comment до labels)

В sense «повилика» / «dodder»: `comment` сразу после `text`, затем `labels`.

## 2026-07-15 — гӏараш: два sense (повилика / Араш)

Склеенный sense `повилика (…) 2 Араш` разбит: ботаника «повилика» + comment;
имя «Араш» с `precomment` `'ГӀ' прописная буква`, comment и sense-forms
`гӏарашица`/`гӏарашил`. Word-level `forms` без изменений. То же в `av-en`
(dodder / Arash).

## 2026-07-15 — гӏарац / Арац: откат правки «прописная буква А»

Откат [`f4f9e9f`](https://github.com/avar-me/sources/commit/f4f9e9f): у имени «Арац»
`precomment` `'ГӀ' прописная буква` и forms `гӏарацица`/`гӏарацил` — та же
конвенция, что у остальных имён с диграфом (text — русская передача без диграфа).
В `av-en` снова `'G' capital letter` + те же forms.

## 2026-07-15 — гӏарац / Арац: прописная буква А

У имени собственного «Арац» (жен.) `precomment` был `'ГӀ' прописная буква`,
тогда как `text` и sense-формы уже на «А» (`Арацица`, `Арацил`). Исправлено на
`'А' прописная буква` (`av-ru`); в `av-en` — `'A' capital letter` и формы
`Арацица`/`Арацил` (было `'G'` + `гӏарацица`/`гӏарацил`).

Проверка всех 140 senses с «прописная буква» в `av-ru`: других случаев, где
sense-формы не совпадают с буквой в `precomment`, нет. Расхождение text
(русская передача без диграфа: Абид, Гази, …) vs буква диграфа (Гӏ, Гъ, …) —
норма словаря, не баг.

## 2026-07-15 — av-en: убрать русскую кириллицу из EN-полей (5 статей)

В свободном английском тексте (`text` / `examples[].en` / `comment` без
`comment_lang: "av"`) не должно быть русской кириллицы — только английский
или аварский в кавычках. Правки: `гвай`, `звеновой`, `катан`, `кету`, `щекъер`.

## 2026-07-15 — новый источник av-en (аварско-английский, ~22 800 статей)

Добавлен `data/av-en.jsonl`: построчный машинный перевод `av-ru` на английский
(multilang.avar.me). Структура 1:1 с `av-ru` (слова, forms, senses, examples.av);
`examples[].ru` → `examples[].en`; pos/form/labels — через glossary. Статус `test`
(черновик, без вычитки носителями). Запись в [`sources.json`](sources.json).

## 2026-07-17 — av-ru: опечатки в ботанических глоссах

Найдены при сверке с русско-аварским справочником растений (`avar_plants.pdf`):
`жанзабил` (имбир→имбирь), `кӏалбухӏ` (редка→редька), `гьургьур`
(кулыбаба→кульбаба), `цӏцӏелчӏахӏ` (гелега→галега), `сенеро` (омега→омег),
`карзама` (слово-метка «ботаника» из text → label), `хӏетӏебагӏар` (мусор в
text, букв. перевод → comment).

Подробности: [`changelog/2026-07-17-av-ru-plant-glosses.md`](changelog/2026-07-17-av-ru-plant-glosses.md).

## 2026-07-16 — av-en: синхронизация с av-ru (RFI буквы А + чистка «выражение»)

Обновлённый `av-en.jsonl`: перенесены сегодняшние правки av-ru. Снята метка
`idiom` (= «выражение») из 1031 sense, восстановлен `pos` (840 noun, 37 verb,
4 adverb) вместо `idiom`; POS/глоссы/опечатки из maintainer-RFI по букве А;
слит дубль `анлънусабго`.

Подробности: [`changelog/2026-07-16-av-en-sync.md`](changelog/2026-07-16-av-en-sync.md).

## 2026-07-16 — av-ru: чистка паразитной пометы «выражение» (pos + labels)

Убрана артефакт-метка `«выражение»` из `labels` (1023 sense + 13 статей; она
нигде не стояла одна, всегда при разг./устар./прост. и т. п.). У 872 одиночных
заголовков восстановлен настоящий `pos` (831 сущ., 37 глаг., 4 нареч.) вместо
`pos: "выражение"`. 90 фразовых устойчивых выражений (через дефис/пробел)
сохранили `pos: "выражение"`. Глоссы/переводы не тронуты.

Подробности: [`changelog/2026-07-16-av-ru-vyrazhenie-cleanup.md`](changelog/2026-07-16-av-ru-vyrazhenie-cleanup.md).

## 2026-07-16 — av-ru: maintainer-RFI (dial-eval, буква А)

Разбор 82 замечаний из `av-ru-maintainer-rfi.jsonl` (gemini dial-eval).
Опечатки, comment числительных, POS/форма, `pos «выражение» → существительное`
для явных существительных, глоссы (`абад`, `ах`, `абго`), фактические числа
(`анлъ` = 6), слияние дубля `анлънусабго`. Отклонены false positives
(`comment_lang: av`, формо-/вариант-заглушки).

Подробности: [`changelog/2026-07-16-av-ru-maintainer-rfi.md`](changelog/2026-07-16-av-ru-maintainer-rfi.md).

## 2026-07-16 — av-ru/av-en: хадуб — с местным падежом (2)

Второй comment «с местный падеж» → «с местным падежом» (sense «после
чего-либо»); в `av-en` — «with the locative case».

## 2026-07-16 — av-en: синхронизация с av-ru (~631 статей)

Обновлён `data/av-en.jsonl` (multilang): подтянуты правки `av-ru` последних
раундов (в т. ч. RFI 12–13, stubs `гӏурхъи`/`зарар`, структура `сантӏизе`/
`хъвазе`/`цебе`/`кибго`…), правки glossary/переводов. В дампе поправлены
регрессии: `гӏараш` «dodder» (не spinning top), `ччумучч` «scoop» (не Soviet),
у `бухизе` comment `'торгӏо' ball`.

## 2026-07-16 — av-ru: анкьго, хадуб

- `анкьго` comment: «при конкретном счёте и в качестве определения»
  (как у `анкьазарго`/`ичӏго` в раунде 13).
- `хадуб` (послелог): «с местный падеж» → «с местным падежом».

## 2026-07-16 — av-ru: образ — comment_lang

`comment`: «художественнияб сипат» + `comment_lang: av`.

## 2026-07-16 — av-ru: бухизе — ответ на загадку

У загадки «буханагӏан басандулеб жо»: comment `'торгӏо' мяч`; отдельный
example `торгӏо`/`мяч` убран.

## 2026-07-16 — av-ru: годекӏан — выходить

examples: «как мне на годекан выходит!» → «как мне на годекан выходить!».

## 2026-07-16 — av-ru: хъвазе — порядок ключей comment

В senses с `comment` ключ сразу после `text` (затем labels/examples).

## 2026-07-16 — av-ru: цебе — прежде чем / перед / у / раньше

У sense с деепричастиями: text «прежде чем, перед»; грамматическая помета
вынесена в comment. У послеложного sense: text «перед, у», comment
«кем-чем-либо, чего-либо». У «раньше»: text «раньше», comment
«кого-чего-либо; например задачу, вопрос».

## 2026-07-16 — av-ru: RFI-раунд 13

Разбор 75 открытых RFI. Опечатки, `comment_lang: av`, два sense у `сантӏизе`,
структура `хъвазе` / `кватӏаризабизе` / `кибго` / числительные `анкьазарго`+`ичӏго`,
отклонённые false positives.

Подробности: [`changelog/2026-07-16-av-ru-rfi-round13.md`](changelog/2026-07-16-av-ru-rfi-round13.md);
rejects — `expert-review-av-ru-issues-2026-07-12-rejected.md`, раунд 13.

## 2026-07-16 — av-ru: гӏурхъи, зарар — варианты написания

Stub-статьи без senses: `гӏурхъи` → see `гӏорхъи`; `зарар` → see `зарал`
(как `баракат`→`баркат`, `дунял`→`дуниял`).

## 2026-07-16 — av-ru: эбелаб — в сочетании

comment: «в сочет. с названиями животных» → «в сочетании с названиями животных».

## 2026-07-16 — av-ru: грамматический класс — римские I/II/III

`1`/`11`/`111` → `I`/`II`/`III` у `щивгоцин`, `щийго`, `щияй`, `щибгоцин`, `щийгоцин`.
У `щибгоцин`/`щийгоцин` сиротский sense с классом в `text` слит с первым
смысловым sense (`comment`), по образцу `щивгоцин`/`щийго`/`щибаб`.
Порядок ключей: `text → comment → (labels) → …` (в т. ч. у `щибаб`).

## 2026-07-16 — av-ru: RFI-раунд 12

Разбор 47 открытых RFI. Опечатки (`парковая`, `программа`, `совок`,
`телефонограмма`, …), `comment_lang: av`, склейка senses у `лъоба`,
структура `гъоркьегӏан` + отклонённые false positives.

Подробности: [`changelog/2026-07-16-av-ru-rfi-round12.md`](changelog/2026-07-16-av-ru-rfi-round12.md);
rejects — `expert-review-av-ru-issues-2026-07-12-rejected.md`, раунд 12.

## 2026-07-15 — av-ru: гъезе — senses по источнику

Разделены диал. «сажать» / «наступать»; помета о наречиях — только `comment`;
управления в comment; убран see_also `откуда-л`; pos → глагол.

Подробности: [`changelog/2026-07-15-av-ru-gyeze.md`](changelog/2026-07-15-av-ru-gyeze.md).

## 2026-07-15 — av-ru: наклонение — изъявительное

examples[1].ru: «изявительное наклонение» → «изъявительное наклонение».

## 2026-07-15 — av-ru: щват-щват — во весь рост

examples[0].ru: «развалился на весь рост» → «развалился во весь рост».

## 2026-07-15 — av-ru: мацӏ — пырей

examples[32].ru: «пирей» → «пырей» (ботаника).

## 2026-07-15 — av-ru: базе — text / comment

Управления и уточнения вынесены из `text` в `comment` по всем senses;
вспомогательный глагол — только comment (без text), с точкой с запятой
перед «с общим значением…». Также `покрыть`/`накрыть` вместо дублей
«покрывать, покрывать» / «накрывать, накрывать».

Подробности: [`changelog/2026-07-15-av-ru-baze.md`](changelog/2026-07-15-av-ru-baze.md).

## 2026-07-15 — av-ru: чухтӏу — надевать чохто

examples[0].ru: «одевать чохто» → «надевать чохто».

## 2026-07-15 — av-ru: порядок ключей — comment до examples

В 25 senses, где `comment` оказывался после `examples` (из-за
json.dumps при правках), ключи переставлены в принятый порядок:
`text → (labels) → comment → (comment_lang) → examples`. Содержание без изменений.

## 2026-07-15 — av-ru: нахъа — послелог

Последний sense (ом. 2): text «за чем-либо, позади кого -чего-либо»;
«с локативом I серии» → comment (арабская 1 → римская I).

## 2026-07-15 — av-ru: кавычки у «X» прописная буква (везде)

Все precomment/comment/text вида `А прописная буква` → `'А' прописная буква`
(и то же для диграфов: `'Гъ'`, `'Къ'`, `'КӀ'`, `'Лъ'`, `'ТӀ'`, `'ЧӀ'` и т. д.).
105 вхождений в 104 статьях; уже закавыченные (`'П'`, `'Х'`, `'ГӀ'` …) не трогали.
Особый: `дибир` — `'Д' прописная буква; имя собств. муж`;
`чергес` (ом. 2) — метка перенесена из `text` в `precomment`, текст «Чергес»;
`аллагь` — ошибочное `'Б'` → `'А'` (по первой букве аварского слова).

## 2026-07-15 — av-ru: кавычки у «Х»/«Хъ»/«ХӀ» прописная буква

precomment: `хамиз`, `харзин`, `хохоч` (`'Х'`), `хъахӏаб`, `хъилич` (`'Хъ'`),
и 12 статей с `'ХӀ'` — по тому же паттерну, что у Гь/П/Къ.

## 2026-07-15 — av-ru: RFI-раунд 11 — опечатки, comment_lang, структуры

Разбор 105 открытых RFI. Исправления в источнике (в т.ч. `батӏалъизе`,
`ревлюц*`→`революц*`, `comment_lang: av`, опечатки) + отклонённые false positives
(см. `expert-review-av-ru-issues-2026-07-12-rejected.md`, раунд 11).

Подробности: [`changelog/2026-07-15-av-ru-rfi-round11.md`](changelog/2026-07-15-av-ru-rfi-round11.md).

## 2026-07-15 — av-ru: рикӏкӏине — считать деньги

«считать денег» → «считать деньги».

## 2026-07-15 — av-ru: зурма — пунктуация

text «зурна;» → «зурна»;
examples[5].comment «чью-либо букв.; . зурну» → «чью-либо; букв. зурну».

## 2026-07-15 — av-ru: басрияб — примета в comment

examples[3]: «примета» вынесено из ru в comment.

## 2026-07-15 — av-ru: белъукъаб — недоваренный картофель

«недоваренная» → «недоваренный».

## 2026-07-15 — av-ru: гама — толокна

«тарелка полная толокном» → «тарелка полная толокна».

## 2026-07-15 — av-ru: гунзари — долихоцефал, comment антр.

examples[2]: «антр долихоцефал» → ru «долихоцефал», comment «антр.».

## 2026-07-15 — av-ru: мацӏ — очистить comment

`senses[0].examples[10].comment`: «); сказать что-либо» → «сказать что-либо».

## 2026-07-15 — av-ru: «'П' прописная буква»

`пазилат`, `пирдавус`: precomment «П прописная буква» → «'П' прописная буква».

## 2026-07-15 — av-ru: караматаб — чудотворный + label книжное

text «книжное чудотворный» → text «чудотворный», labels ["книжное"].

## 2026-07-15 — av-ru: айгъир — жеребца

`senses[0].examples[4].ru`: «…на жеребец» → «…на жеребца».

## 2026-07-15 — av-ru: ретӏел — сшить платье

examples[10].ru: «сжить платье» → «сшить платье».

## 2026-07-15 — av-ru: кӏобокӏахъдизабизе — землетрясение

examples[0].ru: «от землятрясение …» → «землетрясение сотрясло наш дом».

## 2026-07-15 — av-ru: таваккалчӏужу — решительная

text: «решительнаял» → «решительная».

## 2026-07-15 — av-ru: стационарияб — стационарные

examples[2].ru: «стационаные» → «стационарные».

## 2026-07-15 — av-ru: поляк — comment «танец»

examples[0].comment: «); танец» → «танец».

## 2026-07-15 — av-ru: авиабаза — comment_lang av

senses[0].comment «самолётазул база» + `comment_lang: av`.

## 2026-07-15 — av-ru: гӏачивехьлъи — пастушеством

examples[0].ru: «пасттушеством» → «пастушеством».

## 2026-07-15 — av-ru: билет — «в кино»

examples[0].ru: «билет на кино» → «билет в кино».

## 2026-07-15 — av-ru: сундухъ — «за что», локатив от щиб/щал

text «за что»; comment «локативный падеж от 'щиб', 'щал'» (+ locativefrom);
pos местоимение, form локативный (было глагол/инфинитив).

## 2026-07-15 — av-ru: тенкезе — «пускать колкости»

senses[1].text: «пускать колькости» → «пускать колкости».

## 2026-07-15 — av-ru: хӏацӏу — «вытереть слюни»

examples[0].ru: «почистить слюни» → «вытереть слюни».

## 2026-07-15 — av-ru: слюны → слюни (хӏацӏу и др.)

`хӏацӏу`: text и примеры; также пример в `хӏацӏадизе`, `щибниги`.
«о слюнах» → «о слюнях».

## 2026-07-15 — av-ru: гьус — «взять» вместо «взы»

Междометие (науськивание собак): text «взы» → «взять».

## 2026-07-15 — av-ru: масдар — уточнение перевода

text: «масдар, отглагольное имя» → «масдар, отглагольное имя существительное».

## 2026-07-15 — av-ru: чӏобой — диал. масдар от чӏобозе

Убран text «дилл. масдар»; sense: labels масдар/глагол/диалектизм,
`masdarfrom: чӏобозе`, form масдар; see_also чӏобозе kind from.

## 2026-07-15 — av-ru: биххизе + орган (раунд 10, дополнение)

`биххизе`: расстяг*→растяг* (sense[9]); расстер*→растер* (sense[2]); распадать→распадаться,
распал→распался, распала→распалась (sense[8]). `орган` senses[0].comment «идара, учреждение» и senses[1].comment «хасаб газета, журнал» + `comment_lang: av`.

## 2026-07-15 — av-ru: RFI-раунд 10 — опечатки, родп/местнп, comment_lang (~44 правок)

Разбор 161 открытых RFI. Исправления в источнике + отклонены false positives
(см. `expert-review-av-ru-issues-2026-07-12-rejected.md`, раунд 10).
#138 цӏер («из-подо») отклонено как норма.

Подробности: [`changelog/2026-07-15-av-ru-rfi-round10.md`](changelog/2026-07-15-av-ru-rfi-round10.md).

## 2026-07-15 — av-ru: квен-тӏехалъул — род. п. в sense[0], без comment в sense[1]

senses[0]: comment «родительный падеж от 'квен-тӏех'»;
senses[1]: text «продуктовый, продовольственный» (без comment).

## 2026-07-15 — av-ru: кидаго — «с отрицательным глаголом» в comment

senses[1]: text «никогда»; comment «с отрицательным глаголом»
(было склеено в text с опечаткой «с отрицательное …»).

## 2026-07-15 — av-ru: кинаб — два sense (вопросит./относ. и никакой)

Было одно sense со склейкой «…; с отрицательное … глаголом никакой».
Стало: 1) «какой, который; каков» + labels вопросит./относит.; 2) «никакой»,
comment «с отрицательным глаголом». Отрицательных примеров в источнике два.

## 2026-07-15 — av-ru: глаголы/морфо «от X» → comment `от 'X'` (14 статей)

Оставшиеся указатели: без `text`, `comment: "от 'X'"` (морфопометы склейкой,
напр. `от 'квине'; повел.`). У `щинкӏизабизе` и `ана` добавлен `see_also` kind `from`.

## 2026-07-15 — av-ru: указатели «от X» → comment `от 'X'` (+ бакӏалда sense 2)

Производные без абстр-пометы: убран `text` «от X», вместо него
`comment: "от 'X'"` + `see_also` kind `from` (14 статей: бакӏалда, афгъаназул,
бетӏерлъиялда, …). У `бакӏалда` второй sense: text «вместо, за, взамен»,
comment «кого-чего-либо; местный падеж; послелог».
Глаголы с labels понуд./учащ. и формы с пометами «повел./род./…» не трогали.

## 2026-07-15 — av-ru: «букв.» и «напр.» в comment (25+27 правок)

Сокращения без точки приведены к канону: «букв» → «букв.» (25), «напр» → «напр.» (27).
Формы слова «буква» (напр. «арабских букв») не тронуты.

## 2026-07-15 — av-ru: кӏодо — точка с запятой в пословице

`senses[0].examples[4].ru`: «…скандал не делай…» → «…скандал; не делай…».

## 2026-07-15 — av-ru: абстрактные имена на -лъи — comment вместо «от X» (30 статей)

Единый формат вместо `"text": "от X", "comment": "абстр имя"`:
`comment: "абстрактное имя от 'X'"` без `text`, с `see_also` kind `from`.
У `карьеристлъи` добавлена ссылка на `карьерист` (статьи-базы пока нет);
у `футболистлъи` база нормализована к именительному.

Список: [`changelog/2026-07-15-av-ru-abstract-lzhi.md`](changelog/2026-07-15-av-ru-abstract-lzhi.md).

## 2026-07-14 — av-ru: RFI-раунд 9 — опечатки, split гӏанако/−гӏанасеб, comment_lang (~36 статей)

Разбор 107 открытых RFI. 35 исправлены в источнике (+ новая статья `-гӏанасеб`),
4 уже были исправлены ранее (sync lag), 68 отклонены как ложные срабатывания
(см. `expert-review-av-ru-issues-2026-07-12-rejected.md`, раунд 9).

Ключевые реконструкции: склеенная статья `гӏанако` разнесена на анат. «мениск»
и союз `-гӏанасеб`; у `бахъине` senses 13–15 грамматика → `comment` без `text`;
`полюс` (text+электр. comment)/`магъалоченгел`/`хьолбохъ`/`гьеб`/`къачӏай` — text↔comment и masdarfrom.

Подробности: [`changelog/2026-07-14-av-ru-rfi-round9.md`](changelog/2026-07-14-av-ru-rfi-round9.md).

# Changelog

Журнал изменений данных `data/*.jsonl` — источника правды экосистемы avar.me.

Фиксируем здесь **смысловые правки данных** (словарные статьи, формы, метки, корпусы),
а не служебные изменения сайта/шаблонов/CI. Потребители (`forms`, `avar.me`, `corrector`,
`index`) ориентируются по этому файлу, чтобы понимать, что и когда поменялось.

Формат: новые записи сверху, по датам. У каждой записи — дата, краткое описание, затронутый
источник (`av-ru`, `ru-av`, …) и объём правок. Коммит указывается, когда правка уже в `main`.

Крупные правки сопровождаются подробным построчным diff в папке [`changelog/`](changelog/)
(имя файла `ГГГГ-ММ-ДД-<источник>-<тема>.md`) — чтобы корректоры могли сверить каждую статью.

Исходные текстовые словари (`av-ru.dict.txt`, `ru-av.dict.txt`) в репозитории не храним; берём
их при необходимости из [avar-me/avar-russian-dictionary](https://github.com/avar-me/avar-russian-dictionary).

---

## 2026-07-14 — av-ru: выбырать → выбирать (хъирщизе, чӏаразе)

Дочистка: после RFI-раунда 8 в `хъирщизе` остался пример «выбырать лучшее»;
та же опечатка в `чӏаразе` senses[2].text.

## 2026-07-14 — av-ru: кватӏун — вернуть deverbfrom

В первом sense (деепричастие) восстановлено `deverbfrom: "кватӏизе"` — как у остальных
деепричастий (`балагьун`, `кватӏичӏого` и т. д.); после RFI-раунда 8 поле потерялось.

## 2026-07-14 — av-ru: жиб/жив/жий — глоссы в text, класс в comment

Выровнены возвратные местоимения косвенной речи: в `text` только русская передача
(`жиб` — «что он, что она, что оно»; `жив` — «что он»; `жий` — «что она»), метаописание
и Ⅰ/Ⅱ/Ⅲ грамматический класс — в `comment`.

## 2026-07-14 — av-ru: RFI-раунд 8 — опечатки, паттерны, реконструкции (~95 статей)

Разбор 216 открытых RFI (`review.py`). 93 исправлены в источнике, 123 отклонены
как ложные срабатывания (см. `expert-review-av-ru-issues-2026-07-12-rejected.md`, раунд 8).

Основные группы правок:

- Опечатки в `text`/`examples`/`comment` (зниматься→заниматься, карманна→кармана,
  серебрянная→серебряная, милион→миллион, горрила→горилла, и т. д.).
- Структурные: `редкоредколлегия`→`редколлегия`; у `доб` снят пустой sense «Ⅲ»;
  `кисаго` senses[1] → text «ниоткуда» + comment «с отрицательным глаголом»;
  `хатишдизе` → text «стараться» + label «неопределённое местоимение»;
  `кватӏун` senses[0] — только label «деепричастие» + see_also from `кватӏизе`;
  `гипноз` — убран аварский хвост из text, в единственный example: `лъиениги гипноз гьабизе`;
  `будун` — обрезанный text перенесён в comment (…меньше чем 'Дибир');
  `бацӏцӏалъизе` — реконструкция sense + `comment: "букв."`;
  `бакӏ` — расклеен слитый av/ru пример.
- Паттерны: римские классы (`ичӏабго`/`ичӏгояб`/`ичӏавго`/`ичӏайго`/`жиб`/`щиб`/`добго`);
  `comment_lang: "av"` на аварских comment; снят префикс `2 ` у 8 родительных;
  `хъатун` precomment `'Хъ' прописная буква`.

Подробный diff: [`changelog/2026-07-14-av-ru-rfi-round8.md`](changelog/2026-07-14-av-ru-rfi-round8.md).

## 2026-07-13 — av-ru: римские цифры грамматических классов и кавычки у «Х прописная буква» (17 правок)

Донастройка после раунда 7: два паттерна, ранее записанные в rejected.md как «стиль оригинала»,
на самом деле нуждались в точечной правке символов (не переоткрывая сам вопрос):

- Цифры `1`/`11`/`111`, обозначающие римские грамматические классы Ⅰ/Ⅱ/Ⅲ, заменены на настоящие
  символы римских цифр (не буквы `I`/`II`/`III`): `ичӏгояв` (`"1 грамматика кл."` → `"Ⅰ
  грамматический класс"`), `ичӏгояй` (`"11 грамматика кл."` → `"Ⅱ грамматический класс"`, plus
  `text: "трое"` → `"девятеро"` — не совпадало с парной статьёй и собственным примером), `живго`
  (`"1 грамматика сам, он сам"` разделён на `text: "сам, он сам"` + `comment: "Ⅰ грамматика;
  мужчина"`), `щиб` (`"111 грамматический класс"` ×2 → `"Ⅲ грамматический класс"`, уже
  использовался `Ⅲ` в соседнем значении той же статьи).
- Пометка `precomment: "Х прописная буква"` (у собственных имён-омонимов, произведённых от
  диграфа) взята в кавычки во всех 13 статьях с этим паттерном: `"'Гь' прописная буква"` /
  `"'ГӀ' прописная буква"` — `гьитӏин`, `гьитӏинкӏаб`, `гӏабид`, `гӏадил`, `гӏакъил`, `гӏалхулав`,
  `гӏашикъ`, `гӏизраил`, `гӏинкъаб` (два значения), `гӏолохъан`.

## 2026-07-13 — av-ru: RFI-раунд 7, вторая партия — реконструкции и опечатки (10 правок)

Разбор оставшихся 67 из 120 открытых RFI. Большинство (~59) оказались ложными
срабатываниями LLM-инструмента `review.py` (не знает региональной/религиозной лексики,
сравнивает слово с собой же, не видит собственные условности словаря — римские цифры
грамматических классов, пометки «от X»/«абстр имя», «с отрицательное (местоимение)
глаголом X») — подробности и номера RFI в `expert-review-av-ru-issues-2026-07-12-rejected.md`.

Реальные правки:

- `духъ` — статья была слепленной склейкой двух разных слов: местного падежа от «мун» («ты»)
  и энклитической частицы вероятности. Разделена на `духъ` (`text: "у тебя, по тебе, с тобой,
  через тебя"`, `locativefrom: "мун"`, `see_also` → `мун`) и `-духъ` (частица «повидимому,
  чтобы было», по аналогии с другими частицами-энклитиками на дефисе: `-хун`, `-цин`, `-ниги`).
- `тел` (омоним 1, «пролёт») — обрывавшийся на полуслове `text: "...равная 8 натам —"`
  дополнен и перенесён в `comment`: «мера длины чего-либо, равная 8 натам — около 4 метров»
  (расчёт по статье «натӏ»: 6 натов = 3 метра).
- `хъияхъ`: `"полинь"` → `"полынь"` (опечатка в названии растения).
- `багъизе`: `"чуствовать"` → `"чувствовать"`.
- `жаниб` (пример «выходить откуда-л»): `"откуда-л"` → `"откуда-либо"` (обрезанная частица).
- `борцине` (пример «мерить на глаз»): `"букв глазами"` → `"букв. глазами"` (пропущенная точка).
- `лебаллъизе`: `"приучать энергичности"` → `"приучать к энергичности"` (пропущенный предлог).
- `бачине` (senses[9]): восстановлен слитный/опечатанный текст грамматической пометки
  («сдеепричастие др. глагола… значениемучащательности действияза кем-либо» →
  «с деепричастием другого глагола выступает в роли вспомогательного глагола с общим значением
  учащательности действия за кем-либо»), перенесён из `text` в `comment` (это пояснение к
  конструкции, а не перевод). Там же в другом значении: `"окончанием-кать"` → `"окончанием
  '-кать'"` (частица `-кать` не была отделена от слова «окончанием»).

## 2026-07-13 — av-ru: RFI-раунд 7 — опечатки, av-комментарии, реконструкции (52 правки)

Разбор 51 пункта из новой партии RFI от `multilang.avar.me` (120 открытых), с построчным
апрувом каждого пункта пользователем.

Категория A — опечатки в русском переводе/примерах (39 статей): `арбабаш`, `багӏаргӏеч`,
`хьури`, `фотомонтаж`, `спортсмен`, `ахирлъизе`, `борчӏизе`, `невралгиялъул`, `тариф`, `гьумер`,
`гӏамалкӏудияб`, `тӏехӏнису`, `препарат`, `къимат`, `хъазахъ`, `кьур-кьуризе`, `хӏукумат`,
`лъахьи`, `чӏваркьан`, `гӏузру`, `элги`, `тӏвапи`, `сахлъи`, `тенкей` (было «обмен
колькостьями» — на самом деле идиома «обмен колкостями», а не «количествами»), `балагьун`
(частица `ниж` «мы» была отрезана и приклеена к началу русского перевода вместо конца
аварского примера — перенесена на аварскую сторону), `камизе`, `шовинистияб`,
`гӏуцӏиялъулаб`, `сарин`, `кодоб`, `кету`, `бухӏизе`, `мукъсанаб`, `хис-бас`, `бизараб`,
`гъуризе`, `имангури` (`text: "имангуризе"` → `masdarfrom: "имангуризе"`), `куб`
(`"математика куб"` → `text: "куб"` + `labels: ["математика"]`). Статья `плагиатлъи`
(«плагиатство») оставлена без изменений — слово нормальное, RFI отклонён.

Категория B — добавлен `comment_lang: "av"` к аварским толковательным комментариям (8 статей):
`авиазавод`, `футболка`, `прогульщик`, `соната`, `военком`, `граф`, `сенатор`, `фургон`.

Категория C — мелкие реконструкции (4 статьи): `гъоркьарукъ` (текст/комментарий переставлены
местами: `text` → «нижняя комната», `comment` → «на первом (нижнем) этаже»); `кьибил`
(убран мусорный фрагмент `); ` перед «съедобный»); `цӏадалхӏама` (омоним 2 — вынесено
описание обряда вызывания дождя из `text` в `comment`, убраны две лишние точки с запятой,
`text` теперь только «одеяние, сплетённое из тонких зелёных прутьев»); `дирго`
(«протяж местоимение» → «притяжательное местоимение»).

Категория D — исправлен опечатанный заголовок статьи: `инветарь` → `инвентарь` (форма, стем,
все примеры внутри статьи и ссылка в `спорталъул`). Отклонён альтернативный вариант
`звеновод` → `звеньевод` — оставлен как возможная аварская адаптация слова.

## 2026-07-13 — dede-korkut: нормализация палочки (74 из 125 предложений)

При извлечении текста книги из PDF диграфы тӏ/гӏ/кӏ/хӏ/цӏ/чӏ систематически набирались
цифрой `1` вместо палочки (`к1удияб` вместо `кӏудияб`). Прогнан `normalize_palochka()`
по всему `av`-полю `data/dede_korkut.jsonl` — исправлено 74 из 125 предложений.

Отдельно вручную поправлены места, где `normalize_palochka()` спутал бы римское число
с диграфом хӏ (единственная диграф-основа, совпадающая по глифу с римской цифрой X):
`Х1Х гӏасруялъул` → `ХIХ гӏасруялъул` (XIX век); `Т.1`/`Т.III` → `I том`/`III том`;
«Осман I» (`I ГӀусманил`/`I Османил`, 4 места); `Х11 с.48` → `ХII с.48` (соответствует
русскому «т. XII, с.48»).

Правило палочки в [CLAUDE.md](CLAUDE.md) расширено (диграф-основы л/п — почти не
встречаются, не трогать автоматически; римские числа и иноязычная латиница/кириллица —
не диграф, не трогаются или приводятся к латинской `I`, а не к аварской палочке).
`normalize_palochka()` (`src/build_site.py`) доработан: распознаёт римские числа
(`ХIХ`, `ХII`, …) и больше не путает одиночную латинскую `I`/`II`/`III` (том, глава,
имя с числом) с ошибочной цифрой.

## 2026-07-13 — dede-korkut: новый источник, полный объём книги (125 предложений, 5 глав)

Добавлен параллельный корпус `data/dede_korkut.jsonl` — русский и аварский переводы
книги «Китаби-Деде Коркуд» (Баку, Бакинский международный центр мультикультурализма,
2015; оба перевода совпадают постранично, 80 стр.). Включены все содержательные
разделы книги, кроме служебных (именной указатель, список переводчиков, колофон):

- гл. 1 «Вступительное слово» (5 предл.);
- гл. 2 «Огузский циклоп в сравнении с циклопом Гомера» — эссе Г. Ф. фон Дица,
  52 пронумерованных параграфа источника сведены по одному в предложение;
- гл. 3 «Песнь о том, как Бисат убил Тепегёза» — само сказание, русский текст —
  классический перевод акад. В. В. Бартольда (53 предл.);
- гл. 4 «О Дастане «Китаби-Деде Коркуд»» (7 предл.);
- гл. 5 «О Генрихе Фридрихе фон Дице» (8 предл.).

Первая версия источника (коммит 568819a) содержала только сказание и местами
турецкий пересказ (отдельное издание, не постраничное соответствие с ru/av) —
турецкий убран как несовместимый по объёму и структуре с остальными двумя языками,
взамен разметка расширена на всю книгу. Оригиналы — `books/dede_korkut_avar.pdf`,
`books/dede_korkut_ru.pdf`.

Сайт (`src/build_site.py`, `src/templates/styles.css`) расширен для рендера корпусов
с произвольным числом параллельных языков и явными заголовками глав вместо
«Глава N» (используется здесь; у «Тараса Бульбы» поведение не изменилось).

## 2026-07-13 — av-ru: восстановлены разбитые статьи алъ/алъие/алъул (3 правки)

Дореверенный RFI: у `алъ` `text` был обрывком («эрг от») с мусорным примером вместо
падежной формы. `алъ`/`алъие`/`алъул` — общие падежные формы (эргатив/датив/генитив) от
`аб` («это, оно») и `ай` (омоним 2, «она»), см. `forms` в этих статьях. Каждая статья разбита
на два значения — по одному на каждый источник (`аб`/`ай`), вместо одного поля
`ergativefrom: "аб и ай"` (склеенная, нерезолвящаяся ссылка):

- `алъ`: `pos` «послелог»/`form` «—» → `pos` «существительное»/`form` «эргативный»; два значения —
  «она» (`ergativefrom: "ай"`) и «оно» (`ergativefrom: "аб"`); `see_also` на оба `аб` и `ай`.
- `алъие`: два значения — «ей» (`dativefrom: "ай"`) и без перевода (`dativefrom: "аб"`, «оному» не
  является существующим словом); `see_also` на оба `аб` и `ай`.
- `алъул`: два значения — «её» (`genitivefrom: "ай"`) и без перевода (`genitivefrom: "аб"`, аналогично
  «оного»/«ономый» не существует); `see_also` на оба `аб` и `ай`.

## 2026-07-13 — av-ru: опечатка «выкапивать» → «выкапывать» (2 правки)

Исправлена опечатка в корне глагола (перепутан «капать» и «копать»):

- `бахъизе` (sense): `"выкапивать, выкопать"` → `"выкапывать, выкопать"`.
- `къерен` (example): `"выкапивать свинорой пальчатый"` → `"выкапывать свинорой пальчатый"`.

## 2026-07-13 — av-ru: разбор внешнего отчёта issues.av-ru.jsonl, раунд 6 (15 правок из 48)

Пересобран свежий срез открытых RFI из `multilang.avar.me` (командой из протокола в `CLAUDE.md`).
Из 48 записей: 1 (`бер`) оказалась стале-репортом — уже исправлена раньше добавлением `comment_lang: "av"`;
1 (`алъ`, «эрг от») выглядит структурно повреждённой (обрывок текста + явно посторонний пример), но
без чистого источника чинить рискованно — отложена; 31 отклонена как ложное срабатывание (детали и
причины — в [`expert-review-av-ru-issues-2026-07-12-rejected.md`](expert-review-av-ru-issues-2026-07-12-rejected.md), раунд 6, включая `къадар` — по прямому решению пользователя). Исправлено 15 явных опечаток/структурных мелочей:

- букв. без точки → с точкой: `махх` («букв железом»), `нусго` («букв дымов»);
- опечатки набора: `калпатгъецӏ` (гвозд→гвоздь), `тӏагъур` (позвращают→возвращают), `байрам` (частвовать→участвовать), `агъазгьечӏеб` (безпечный→беспечный), `сон` (передыдущий→предыдущий), `юхъарухъан` (польностью→полностью), `кьерханкъоялъ` (каменнную→каменную), `закагӏат` (объязан→обязан);
- `вабабай`: убрана лишняя точка с запятой внутри фразы («сильное; волнение» → «сильное волнение»);
- `санузел`: аварское окончание в русском тексте («санитарияб узел» → «санитарный узел»);
- `гьатӏанрукъ`: ответ на загадку взят в одинарные кавычки как аварское слово («хоб могила» → «'хоб' могила»), по уже принятому правилу (см. «бер», «цӏад»);
- `ноктюрн`: `comment` был полностью на аварском без пометки — добавлен `comment_lang: "av"`;
- `ункъгӏаллъи`: убран избыточный `text: "от ункъгӏал"`, дублирующий уже имеющиеся `comment` + `see_also` (тот же случай, что `чӏухӏдаризабизе`/`къуш-къушизе`).

## 2026-07-13 — av-ru: прогон hunspell, раунд 2 — «высокая уверенность», count=2 (40 статей, 61 правка)

Продолжение разбора hunspell-отчёта (`review/hunspell-av-ru-2026-07-13-high-confidence.tsv`):
пройдены все 149 слов с `count=2` (встречаются в двух полях словаря); 3 уже были исправлены
в предыдущем раунде (дефисы в цветовых прилагательных). Из оставшихся подтверждено и исправлено
40 статей (61 вхождение) явных опечаток набора текста:

- обяснять/обясниться/обяснить/рязъяснить/рязъясниться → объяснять/объясниться/объяснить/разъяснить/разъясниться (`багьизабизе`, `багьизе`, `бичӏчӏизабизе`);
- уменшить/уменшиться → уменьшить/уменьшиться (`гьитӏинлъизабизе`, `дагьлъизабизе`, `дагьлъизе`, `тӏадагьлъизе`);
- стеревятник → стервятник (`балкан`, `чудук`); карноухий → корноухий (`гӏин`); кровохаркания → кровохарканья (`би`);
- вышивальшица → вышивальщица (`угьаризе`, `угьарухъан`); взырая → взирая (`гьумер`); этых → этих (`зодиб`, `кинабаб`);
- домотканная → домотканая (`гӏарщ`, `къвал`); тиквы → тыквы (`гӏарщ`);
- дьяволский/дьяволские → дьявольский/дьявольские (`шайтӏанаб`, `шайтӏаналъул`);
- замужная/замужную → замужняя/замужнюю (`ригьин`, `росасул`);
- сумашедшим → сумасшедшим (`гӏадаллъизабизе`, `гӏадаллъизе`); набрасиваться → набрасываться (`гъанчдезе`, `тӏадекӏанцӏизе`);
- телячим → телячьим (`бачавехь`, `бачавехьлъи`); утихомировать → утихомиривать (`гӏодоцинабизе`, `гӏодоцине`);
- рассточительность/рассточительство → расточительность/расточительство (`исрап`, `исраплъи`);
- заплесневать → заплесневеть (`ккал`, `ккалазабизе`); мылостиню/мылостыню → милостыню (`къакъуцӏ`, `къо`, `гьаризе`, `бахъизе`);
- изьян → изъян (`мукъсанлъи`); поздный → поздний (`ригьгьечӏуда`); похлёпка → похлёбка (`карщ`, `хузе`);
- краткошеий → короткошеий (`горбокъаб`); длиношеий → длинношеий (`габур`, `габурхалат`);
- многожды → многажды (`гӏемерцӏул`, `чанго`); ньюхательный → нюхательный (`сунтӏ`, второе вхождение — первое поправили в прошлом раунде).

У `гӏодоцине` («успокаиваться, успокоиться; ~») исходная пара «утихомировать, утихомирить» и без
опечатки смысловая нестыковка (глагол непереходный, а пара переходная — видимо, копипаста из
`гӏодоцинабизе`); поправлена только орфография, смысловой вопрос не трогали.

Остальные ~106 слов из выборки `count=2` — ложные срабатывания (личные имена, этнонимы, диалектизмы
и специальная лексика: чагур, чауш, шаъбан/шаабан, сафар, зурнач, тухум, урбеча, кремнёвки, наибство
и т. п. — реальные слова вне словаря hunspell) или ложные разбиения составных слов инструментом
(«тух умом», «пол саха», «мало урожайность»). Не тронуты.

## 2026-07-13 — av-ru: разбор внешнего отчёта issues.av-ru.jsonl, раунд 6 (3 правки)

Проверены оставшиеся открытые замечания (RFI) автоматического ревью. Исправлено 3:

- `гӏатӏилъизе`: «в комнете стало просторно» → «в комнате стало просторно» (опечатка).
- `цохӏо`: «мы живём одным домом» → «одним домом» (опечатка).
- `фермер`: `comment` («жиндирго ферма бугев чи») был на аварском без метки — добавлен `comment_lang: "av"`.

18 замечаний отклонены как ложные срабатывания (законная лексика/грамматика, сравнение слова с
собой, транслитерация безэквивалентных диалектных терминов, уже помеченные архаизмы) — добавлены
в [`expert-review-av-ru-issues-2026-07-12-rejected.md`](expert-review-av-ru-issues-2026-07-12-rejected.md),
раунд 5. `бер` («гулгун кумган») не тронут — уже получил `comment_lang: "av"` в недавней
реконструкции статьи, RFI закроется сам при следующей синхронизации.

## 2026-07-13 — av-ru: разбор внешнего отчёта issues.av-ru.jsonl, раунд 5 (7 правок)

Проверены следующие открытые замечания (RFI) автоматического ревью. Исправлено 7:

- **5 опечаток**: `трамплин` («лижный трамлин» → «лыжный трамплин»), `махӏу` («выше шиколотки до колена» → «щиколотки»), `къвачӏакӏутӏ` («последный ребёнок» → «последний»), `щуцӏали` и `щуцӏалиялда` (обе статьи диалектного «пятьдесят»: «пятдесять» → «пятьдесят», 3 вхождения в тексте/примерах).
- **2 структурные правки** (тот же баг, что чинили в `мисал`/`чӏухӏдаризабизе`): `мина-минатӏавус` (`text: "ботаника пушица"` разделён на `text: "пушица"` + `labels: ["ботаника"]` — «пушица» реальный ботанический термин, Eriophorum), `къуш-къушизе` (убран избыточный `text: "от къушизе"`, дублирующий уже имеющиеся `labels: ["учащательная форма"]` + `see_also`).

4 замечания отклонены как ложные срабатывания (`рикьи` — «ритль» реальная мера сыпучих; `бикъизе` — «из дому» нормальная устойчивая форма; `къаргъа` — «каргу» это историческая игра с мячом, а не опечатка; `чӏвагъ` — «охвостье» совпадает с переводом заголовка статьи) — добавлены в [`expert-review-av-ru-issues-2026-07-12-rejected.md`](expert-review-av-ru-issues-2026-07-12-rejected.md), раунд 4. Туда же добавлено ранее пропущенное `росасул` («мужнина родня» — архаичное притяжательное, согласовано с «мужняя жена» в той же статье).

## 2026-07-13 — av-ru: мисал — дополнена грамматическая помета управления

В примере «мисал босизе» (брать пример, подражать) `comment` был неполным («кого-чего-либо» —
относился только к «брать пример»). По оригиналу словаря («брать пример с кого-чего-л., подражать
кому-чему-л.») дополнен до «с кого-чего-либо, кому-чему-либо», отражая оба управления глагола.

## 2026-07-13 — av-ru: разбор внешнего отчёта issues.av-ru.jsonl, раунд 4 (7 правок)

Проверены новые открытые замечания (RFI) автоматического ревью после раунда 3. Исправлено 7 явных опечаток набора текста:

- `кӏикъоялда` (comment): убран мусорный вставленный фрагмент «и 50 0009» из перечисления диапазонов числительных.
- `мисал` (sense 2): помета «мат.» была слеплена с обрывком «скому-чему-либо» (утёкшим из соседнего значения) прямо в `text` — по чистому источнику, присланному пользователем, восстановлено: `text: "пример"`, `labels: ["математика"]`.
- `рехунтезе`: «брошеная» → «брошенная».
- `кьер-кьераб`: «разные фукты» → «разные фрукты».
- `къахӏазе`: «крепко держатья» → «крепко держаться».
- `консерватория`: «студент косерватории» → «студент консерватории».
- `квешлъи`: «испытиват, испытать нужду» → «испытывать, испытать нужду».

Остальные ~21 открытых замечания этого раунда — ложные срабатывания инструмента (сравнение слова с собой, региональная/специальная лексика, аварская графика) или требуют дополнительной сверки с источником; не тронуты, ждут отдельного разбора.

## 2026-07-13 — av-ru: полная реконструкция статьи «бер» (глаз) по чистому источнику

Статья `бер` (омоним 1, «глаз») была тяжело повреждена: `senses[0].text` содержал перевод «глаз, око» слитый без пробелов с десятками меток и обрывков комментариев, принадлежавших разным примерам. Полностью переписана по чистому тексту словаря, присланному пользователем (139 → 141 пример). Заодно исправлено то, что было потеряно/искажено в старой версии:

- добавлены пропущенные вторые значения: «бер бухьине» → «гипнотизировать»; «берал къанщизе» → «умереть» (в переносном значении);
- восстановлены пропущенные пометы `посл.`/`погов.`/`бран.`: бералда бихьараб — битӏараб…, бералда бихьулаго гьереси бицуна, берал къватӏире кӏанцӏаги дур, берал нухъица рахъайги дур, берал руго — канлъи гьечӏо…, берал рукӏаралъур картӏал хутӏаги;
- восстановлены пропущенные пояснения-управления: «бер тӏаса босичӏого валагьизе» (на кого-что-либо), «бералъ босизе» (на ком-либо), «бераздаса канлъи босизе» → выходить из себя (от гнева), «бераздаса канлъи ине» → разозлиться (в переносном значении);
- «букв.» переведено из голого сокращения в полноценный comment («бер гӏорцӏизе», «бер кӏутӏарабго лъана», «бераз кӏкӏвине»);
- убраны артефакты слипшихся пробелов («древниеаварцы» → «древние аварцы», «уженщин» → «у женщин», лишняя запятая перед «гъосда бер чӏвазе»), исправлена опечатка «искуственный» → «искусственный»;
- добавлен `comment_lang: "av"` для полностью аварского комментария-ответа на загадку «гулгун кумган».

## 2026-07-13 — av-ru: прогон hunspell по русской части словаря (44 статьи)

Впервые прогнали hunspell (словарь ru_RU из LibreOffice dictionaries) по всем русским полям (`text`/`comment`/`example.ru`) всего файла — 67 412 полей, 4044 уникальных слов вне словаря hunspell. После отсева служебных обрывков, собственных имён и известной региональной/специальной лексики руками разобрано и исправлено:

- **8 кластеров опечаток** (37 правок в 29 статьях): взайми→взаймы (базе, нахъа, сахӏ), музыцировать→музицировать (бакънагъине, макънагъине, музыка, мурзигат), чтобь→чтоб (бетӏергьанчихвад, бетӏеркъотӏ, гъоркьмес), распросы→расспросы (гьикъа-бакъи, гьикъари, гьикъ-бакъ, гьикъ-цӏех, хӏисаб-суал, цӏеха-рехей, цӏех-рех), сумашедший→сумасшедший (гӏабдалхана, далухана, гӏадалаб), вспылчивый→вспыльчивый (манзилкъокъаб, хехаб, хехдаризе), ньюхать→нюхать (сунтӏ, сунтӏизабизе, сунтӏизе), четырмя→четырьмя (ункъабго, ункъо, чӏвазе).
- **Дефис в составных цветовых прилагательных** («ярко-»/«светло-» + цвет): яркозелёный, яркоголубой, яркокрасный, яркожёлтый, яркобурый, светлозелёный, светлоголубой, светлосерый, светлокоричневый — 88 правок во всех падежных формах (33 статьи семейства канчӏ-/кантӏогьил-), включая попутную опечатку «светлокричневым/-ый» → «светло-коричневым/-ый».
- **12 статей с аварским определением, слитым в `text` без `comment_lang`** — тот же баг, что чинили у парфюмерии/федерализма: панно, пансион, пантеон, папирус, параллель, парк (омоним 2), патриарх, пергамент, планетарий, планшет, проекция, топография. Объединены в один (или два, если значения различались по labels) `sense` с `comment_lang: "av"`.
- **гьатӏан** (омоним 1): опечатка «церкьовь» → «церковь» (попала в выборку случайно, из-за похожести на аварский диграф).
- **гъата-гъатахинабизе**: убран избыточный `text: "от гъата-гъатахине"` — значение уже полностью выражено `labels`+`see_also` (тот же случай, что чӏухӏдаризабизе).

**Отложено**: статья `бер` (омоним 1, глаз) — `senses[0].text` сильно повреждён (перевод «глаз, око» слит без пробелов с десятками меток и обрывков от отдельных примеров); требует отдельной вдумчивой реконструкции, как раньше `бихьизе`.

Ещё ~1300 «высокой уверенности» и ~2400 «низкой уверенности» кандидатов от hunspell по всему файлу не разбирались — оставлены для будущих раундов.

## 2026-07-13 — av-ru: опечатка «подзатильник» → «подзатыльник» (2 статьи, 4 правки)

Найдена при обсуждении сироток `see_also`. Исправлено в `гъванща` (3 вхождения: «давать подзатыльников», «надавать подзатыльников», пословица «из-за языка подзатыльники получишь…») и `апас` (1 вхождение: «дать подзатыльник»). В статье `гъванща` рядом уже был и правильный вариант «дать подзатыльник» — опечатка касалась только части примеров.

## 2026-07-13 — av-ru: разбор внешнего отчёта issues.av-ru.jsonl, раунд 3 (17 замечаний, 9 правок)

Проверен третий присланный отчёт (17 замечаний). Исправлено 9 записей:

- **7 опечаток**: `рохьил` (пропущена точка в «букв.»), `яц-вац` («сесра»→«сестра»), `мухӏкан» («обяснить»→«объяснить»), `сон` («каждий»→«каждый»), `ялъачи` («употреляемое»→«употребляемое»), `чӏамизабизе` (в понудительной форме «искусать»→«испытать» — согласовано со значением базового глагола чӏамизе «вкушать, натерпеться»), `карагьатаб` (помета «книжное» слеплена с переводом в `text` — вынесена в `labels`).
- **По уточнению пользователя**: `зоб` (изменён порядок слов «небо разорвавший скандал»→«скандал разорвавший небо»), `болъонил` (опечатка в ботаническом термине «главилат»→«гравилат», реальное название растения — гравилат городской).
- **9 замечаний отклонены** как ложные срабатывания (2 из них повторяют уже закрытые вопросы из прошлых раундов — дунявияб, масс) — добавлены в [`expert-review-av-ru-issues-2026-07-12-rejected.md`](expert-review-av-ru-issues-2026-07-12-rejected.md), раунд 3.

## 2026-07-13 — av-ru: местоименная парадигма — полный проход по файлу (30 статей, 40 правок)

Полный regex-проход по всему `av-ru.jsonl` на предмет тех же трёх паттернов, что уже чинили в местоименной группе (статья-сирота с одним классом Ⅰ/Ⅱ/Ⅲ, помета «выступает определением…» слитно с переводом, префикс «Ⅰ./Ⅱ./Ⅲ.» перед переводом), нашёл ещё 30 незамеченных слов. Все 40 правок применены:

- **Статьи-сироты** (класс+«указат.» без перевода, слиты с соседним значением + метка «указательное»): `гъоб`, `гъой`, `лъой`.
- **Помета слитно с переводом** (разделены на `text`+`comment`): `гъоб`, `гъой`, `гъойго`, `гъол`, `гъолго`, `гьагъаб`, `гьагъав`, `гьагъай`, `гьагъал`, `гьадаб`, `гьадай`, `гьадал` (оба значения, включая обратный порядок с висячей скобкой), `гьадинаб`, `гьай` (омоним 2), `гьал`, `гьей`, `гьейго`, `гьел`, `дойго`, `дол`, `лъойго`, `лъолго`.
- **Префикс «Ⅰ./Ⅱ./Ⅲ.» перед переводом** (вынесен в comment): `эб`, `эбги`, `эбгияли`, `эбго`, `эй`, `эйги`, `эйгияли`, `эйго`.

Попутно найдены и исправлены смежные ошибки:
- `гъойго` и `лъойго`: перевод «тот же» исправлен на «та же» (класс Ⅱ — женский род, что подтверждают примеры «та же девушка», «через ту же девушку»).
- **Опечатка в заголовке статьи `гъагъай` → `гьагъай`**: слово из серии «находящийся внизу» (`гьагъаб`/`гьагъав`/`гьагъай`/`гьагъал`) было записано без «ь» в основе, при этом `гьагъаб` и `гьагъал` уже ссылались через `see_also` на несуществующее «гьагъай». Заголовок, `forms`, примеры и `see_also` приведены к единому виду; заодно у `гьагъав` исправлены битые ссылки `see_also` (`гьагьаб`/`гьагьай` → `гьагъаб`/`гьагъай`).
- `эй`: восстановлена пропущенная первая буква в примере (`"й жакъа..."` → `"эй жакъа..."`), убран необъяснимый хвостовой символ «~» в переводе.
- `эбго`: опечатка «одно и тоже» → «одно и то же» (иначе меняется смысл на «также»).
- `гьел`: та же опечатка «к с» + лишняя «;» в comment, что уже чинили в `гьелго`.
- `дойго`: `pos` исправлен с «прилагательное» на «местоимение» (согласовано с соседними словами парадигмы).

## 2026-07-13 — av-ru: местоименная группа доразобрана, дунявияб уточнён (16 статей)

Вернулись к отложенному из прошлого раунда: 10 статей местоименной группы (`гъов`, `гъовго`, `гьав`, `гьадав`, `гьев`, `гьевго`, `дов`, `дой`, `лъов`, `лъовго`) получили ту же правку, что раньше `лъол`/`гьелго` — грамматическая помета «выступает определением…» вынесена из `text` в `comment`. Заодно обнаружилось и исправлено 3 разных способа записи грамматического класса Ⅰ/Ⅱ/Ⅲ (не путать с римской цифрой-опечаткой — здесь это настоящий аварский грамматический класс): статья-сирота только с классом+«указат.» (`гъов`, `лъов`, `лъоб` — слита с соседним значением, добавлена метка «указательное»), «указат. Ⅰ» слитно с переводом (`гъовго`, `лъовго` — разделено), префикс «Ⅰ.» перед переводом (`эв`, `эвги`, `эвгияли`, `эвго` — вынесен в comment). У `лъоб` дополнительно разделены текст перевода и грамматическое пояснение «указывает на предмет, находящийся выше говорящего…» в третьем значении. `дунявияб`: перевод «ближайшая жизнь» оставлен как есть (это осознанный дословный перевод коранического термина «ад-дунья», используемый и переводчиками Корана) — добавлен comment с пояснением и упоминанием противоположного понятия «последняя жизнь» (аль-ахира).

## 2026-07-13 — av-ru: разбор внешнего отчёта issues.av-ru.jsonl, раунд 2 (78 замечаний)

Проверен второй присланный отчёт (78 замечаний, часть пересекалась с раундом 1). Итог:

- **21 опечатка и 4 статьи с `comment_lang: "av"`** исправлены (список — в diff репозитория).
- **Структурные правки**: `гӏажизлъизабизе` (заглушка `senses[0].text: "ронуд."` убрана, метка «понудительная форма» перенесена, добавлен `see_also` на `гӏажизлъизе`), `федерализм` и `парфюмерия` (аварское определение из `text` перенесено в `comment`+`comment_lang`, объединены дублирующиеся senses), `чу` (обрубленная фраза «...или» дособрана из соседнего `comment`), `кенчӏезабизе` (метка «понудительная форма» вынесена из текста в `labels`).
- **Полностью переписана статья `бихьизе`** по чистому исходнику словаря, присланному пользователем — старая версия была тяжело повреждена (слипшиеся без пробелов пометы и комментарии в одном из значений). Заодно удалена почти пустая статья-дубль `бихьизе` (только `stem`/`forms`, без единого значения).
- **Точечные решения по указанию пользователя**: `бахӏри` (сокращение «презр» развёрнуто и вынесено в `comment`), `якъия` (перевод заменён на «полугарнец» и в тексте, и в примере), `гьелго` (обе статьи разбиты на чистый перевод + `comment` с грамматической пометой), `гъорлъетӏам` (добавлено «чьему-либо»), `дихъ` (убран необъяснимый обрывок «3; » перед «местный падеж»), `чӏухӏдаризабизе` (убран избыточный `text: "от чӏухӏдаризе"`, дублирующий уже имеющиеся `labels`+`see_also` — новое правило записано в `CLAUDE.md`).
- **44 замечания отклонены** как ложные срабатывания — добавлены к [`expert-review-av-ru-issues-2026-07-12-rejected.md`](expert-review-av-ru-issues-2026-07-12-rejected.md) (теперь накопительный список по всем раундам).
- **Оставлено на потом**: `дунявияб` (перенесено из прошлого раунда без изменений) и группа местоименных статей (`гъов`, `гьав`, `дов` и др.) с тем же паттерном «выступает определением...», что чинили в `гьелго` и `лъол`, — решение по всей группе сразу отложено на будущее.

## 2026-07-12 — av-ru: аварские слова в comment — в одинарные кавычки (23 статьи)

Новое правило оформления (записано в `CLAUDE.md`): если внутри русского `comment` встречается аварское слово (ответ на загадку, форма, от которой образовано слово), оно берётся в одинарные кавычки — чтобы отличать от русского текста. Применено к `диде` (comment переписан: text «от дун» → «меня», comment уточнён с кавычками вокруг «дун» и падежных форм), `бох` (загадка про молоток) и ещё 20 статей-загадок, найденных тем же проходом (`ахӏдезе`, `бакъдабжо`, `бегизе`, `бегьизе`, `бежен`, `бецӏаб`, `босизе`, `габур`, `гъадаро`, `гьава`, `гӏин`, `кигӏан`, `къаси`, `кьарияб`, `кӏудияб`, `лъим`, `мегӏер-гӏалах`, `хадулъугьине`, `хан`, `хъерело`, `чӏер`). Заодно почищены 2 старых артефакта `); ` в начале comment (`бегизе`, `бежен` — тот же баг, что уже правили в `бегьизе`). Также: `пикет` sense#2 получил `comment_lang: "av"` (второй смысл статьи тоже был на аварском); в `бетӏербахъи` убран лишний `? —?` в переводе примера.

## 2026-07-12 — av-ru: разбор внешнего отчёта issues.av-ru.jsonl (106 замечаний)

Проверен присланный отчёт автоматического ревью (106 замечаний). По каждому — сверка с данными вручную:

- **63 статьи исправлены**: 54 опечатки/грамматика, 9 статей получили `comment_lang: "av"` (`капитан`, `пациент`, `пулуша`, `облицовщик`, `нефрит` омоним 1, `сирена`, `протестантство`, `номинатив`, `параноя`).
- **6 статей перестроены структурно**: `нужум` (метка «книжное» вынесена из текста перевода в `labels`), `мохмох` (обрывок идиомы перенесён в комментарий примера-пословицы), `пикет` (аварское толкование вынесено в `comment`+`comment_lang`), `капкикълъизабизе` (метка «понудительная форма» перенесена в `labels`, добавлена ссылка `see_also` на базовый глагол `капкикълъизе`), `лъол` (грамматическая помета вынесена в `comment`, текст перевода очищен до «те»), `нигагьлъун` (уточнение «своё»).
- **2 статьи оставлены без изменений по прямому указанию** (`дунявияб`, `якъия`) — под вопросом, требуют дальнейшей сверки с источником.
- **33 замечания отклонены** как ложные срабатывания инструмента — полный список с обоснованием в [`expert-review-av-ru-issues-2026-07-12-rejected.md`](expert-review-av-ru-issues-2026-07-12-rejected.md), чтобы не пересматривать те же случаи повторно.

Заодно задокументировано в `CLAUDE.md`: правило распознавания понудительной формы глагола по суффиксу `-забизе`/`-абизе`.

## 2026-07-12 — av-ru: comment_lang для заимствованных слов (661 статья)

Продолжение находки `comment_lang` (записи выше): полный проход по файлу нашёл ещё 661 статью (660 + `кардинал`) с той же проблемой — `comment` на аварском вместо русского. Основной паттерн (621 из 661): статья на заимствованное/интернациональное слово, где `text` совпадает с заголовком (перевод не нужен — слово то же самое), а `comment` даёт аварское толкование понятия (`кардинал` → «католиказул диналъул бищун кӏудияв чи»). Остальные — то же самое с небольшими отличиями в `text`. При поиске отсеяны 2 ложных срабатывания: `дохӏодин` (comment на русском, содержит аварскую частицу «-хӏо-» в дефисах) и `милица` (comment на русском, содержит римскую цифру «ХӀХ» = XIX век, записанную с палочкой). Подробности и полный diff — в [`changelog/2026-07-12-av-ru-comment_lang-batch.md`](changelog/2026-07-12-av-ru-comment_lang-batch.md).

## 2026-07-12 — av-ru: исправлены опечатки в русских переводах

В статье `дуниял`: «выдавший виды» → «видавший виды» (устойчивое выражение); «всю речку выпит — жажду не утолит» → «всю речку выпьет — жажду не утолит» (неверная форма глагола). В статье `кӏалалъ`: «одну чарку выпит — упадёт у очага, две чарки выпит — упадёт у дверей» → «выпьет» в обоих случаях (та же опечатка).

## 2026-07-12 — av-ru: аварский текст из text перенесён в comment_lang (2 статьи)

Ещё 2 находки того же рода, что и `comment_lang` выше, но текст на этот раз оказался в `text`, а не в `comment`: `интервью` — аварское пояснение «(газет-журналалда бахъизе мухбирас цо лъилгунниги гьабулеб гара-чӏвари)» было в скобках прямо в `text` вместе с хвостом мусора `; .`; `стажёр` — аналогично, «(жив ругьун гьавулев чи)» в скобках внутри `text` вместе с русской парной формой «стажёрка». Оба перенесены в `comment` с `"comment_lang": "av"`, `text` очищен до собственно русского перевода (`"интервью"`, `"стажёр; стажёрка"`).

## 2026-07-12 — av-ru: римская цифра Ⅰ вместо палочки внутри диграфа (593 статьи)

Побочная находка при работе над `comment_lang` (выше): во многих статьях внутри аварского диграфа вместо кириллической палочки (`Ӏ`/`ӏ`) стоит похожий по начертанию символ — римская цифра «I» (`Ⅰ`, U+2160), например `чⅠарав` вместо `чӏарав`. Проверено по контексту: заменена только там, где `Ⅰ` идёт сразу после буквы диграф-основы (т, г, к, х, ц, ч) — 593 статьи из 641 найденного вхождения. Остальные 19 вхождений (в `av-ru`) и все 34 (в `ru-av`) — настоящие римские цифры (грамматическая нумерация значений в местоименных статьях, века вида «ⅩⅤ–ⅩⅤⅠ») и не тронуты. Подробности и полный diff — в [`changelog/2026-07-12-av-ru-roman-numeral-palochka.md`](changelog/2026-07-12-av-ru-roman-numeral-palochka.md).

## 2026-07-12 — av-ru: новое поле `comment_lang` (16 статей)

Добавлено новое необязательное поле схемы `sense.comment_lang` (см. [`sources.json`](sources.json) → `schema.sense`): указывает язык поля `comment`, когда он не русский. Причина: в 16 статьях `comment` по ошибке источника содержит аварский текст вместо обычного русского (обнаружено по отчёту `reports/non-russian-source.md` из `multilang.avar.me`). Проставлено `"comment_lang": "av"` у затронутых статей: `антифашист`, `неологизм`, `норма`, `общественник`, `ода`, `олеандр`, `памфлет`, `пародия`, `плеврит`, `публицистика`, `реакция`, `реплика`, `скафандр`, `такса`, `таможня`, `фтизиатр`. Сам текст `comment` не менялся — только добавлена пометка языка. **Потребителям**: поле необязательное и обратно совместимое — если вы просто отображаете `comment` как есть, ничего менять не нужно; если хотите не путать читателя аварским текстом под видом русского комментария, проверяйте `comment_lang` и, если он присутствует и не `"ru"`, либо скрывайте `comment`, либо помечайте его явно как текст на другом языке.

## 2026-07-07 — av-ru: опечатки в русских переводах

Точечные правки в `data/av-ru.jsonl`:

- `предпиятие` → `предприятие` (5 примеров в статье `предприятие`).
- `бить` → `быть` (20 мест в 19 статьях): `гьабудизе`, `гӏайиб`, `чалухъго`,
  `бачавехь`, `бухьине`, `гъара`, `гьайбалъизе`, `гьогьомизе`, `жем-жемизе`,
  `къакъадизе`, `къварилъизе`, `кьегӏер`, `кьеризе` (2×), `кӏигьумерлъизе`,
  `лъикӏаб`, `мацӏ-махх`, `нахъе-цебе`, `талихӏкъине`, `тушманлъи`, `чӏанкӏизе`,
  `чӏегӏер`.

## 2026-07-04 — ru-av: доуборка мусорных see_also и переразбор примеров (14 статей)

Небольшое продолжение чистки see_also после массового обновления: убраны ещё несколько мусорных targets, выдранных из идиом/примеров как вторая ссылка (`возражать`→`вы`, `изныть`→`вся`, `отрываться`→`читать`, `учитывать`→`учитываая`), текст перенесён в нормальные `av`/`ru`-примеры вместо потери; убраны лишние `see_also` типа `from`, указывавшие на случайные слова из примера (`картинка`, `хвостик`, `часок`); развёрнуты сокращения меток («знач. вводн.» → «вводное слово», «значение») в `впрочем`, `поди`. Исключена и восстановлена 1 статья — `изображать` снова принесла уже отклонённый ранее баг («картина» → «карти на»), похоже апдейт собран не от последней версии файла. Подробности и полный diff — в [`changelog/2026-07-04-ru-av-see_also-cleanup-round4.md`](changelog/2026-07-04-ru-av-see_also-cleanup-round4.md).

## 2026-07-04 — ru-av: пробел после запятой (231 статья)

Апдейт от администратора: добавлен пропущенный пробел после запятой внутри `comment`/`text` (224 статьи из 235 присланных), плюс несколько бонусных точечных фиксов — пробел внутри диграфа (`т Ӏад`→`тӏад`) и после инициала (`И.Гончаровасул`→`И. Гончаровасул`). При проверке найдены и исключены из апдейта 4 статьи с посторонними повреждениями, не связанными с запятыми: `изображать` (пропал перевод примера), `к(ко)` (метка «дательный падеж» превратилась в текст + осиротевшую метку «падеж»), `помниться` (лишняя метка «число»), `усики` (`«у сына»`→`«у сы на»`, слово разорвано). Эти 4 восстановлены до правки. Подробности и полный diff — в [`changelog/2026-07-04-ru-av-comma-spacing.md`](changelog/2026-07-04-ru-av-comma-spacing.md).

## 2026-07-04 — ru-av: доработка see_also после массового обновления (4238 статей)

Продолжение записи ниже. У первого апдейта был баг в генераторе `see_also`: пробелы/дефисы внутри целевого слова (`выболтать` → `вы болтать`), аварский текст вместо русского в target, мусорные фрагменты из идиом как второй target. Три раунда багрепортов и фиксов от администратора — итог: подавляющее большинство (190+) исправлено, плюс попутно почищено ~70 более старых битых ссылок, не относящихся к этому апдейту. Не исправлены (оставлены разработчиками на потом, другие причины): `кончаться`, `обвивать(ся)`. При проверке финальной версии найдены и исправлены нами 3 новых регрессии в примерах, зацепленные фиксом-у-источника (`доходить`, `переступать`, `сходить`). Подробности и полный diff — в [`changelog/2026-07-04-ru-av-seealso-fixes.md`](changelog/2026-07-04-ru-av-seealso-fixes.md).

## 2026-07-04 — ru-av: массовое обновление статей (4242 статьи)

Апдейт от администратора: числовые сноски-отсылки внутри примеров (`{"av": "1", "ru": "дрожать"}`) преобразованы в структурированное поле `see_also` с `refwordnum`; составные значения разбиты на отдельные `senses`; исправлены опечатки и склеенные слова (пропавшие пробелы); добавлены отсутствовавшие переводы. Подробный построчный diff по всем 4242 статьям — в [`changelog/2026-07-04-ru-av-batch-update.md`](changelog/2026-07-04-ru-av-batch-update.md).

При проверке перед публикацией найдены и исправлены 2 случая порчи данных во входящем апдейте: у статьи `обязательный` пропало поле `word` и обрезались приставки в примерах («обязательное»→«ое»), у `черносмородинный` перевод обрезался до «новый» и добавилась мусорная форма-перенос строки «черносмороди-». Оба восстановлены до правки. Подробности — в файле diff'а.

## 2026-07-03 — источник ihdal влит в hakikat

Отдельный источник `ihdal` (один номер газеты «ХӀакъикъат», 11 материалов) объединён с `hakikat` (архив статей сайта hakikat.info) — это один и тот же номер, большая часть которого уже была на сайте. Проверка по 11 материалам показала: 8 уже есть в `hakikat.jsonl` (опубликованы на сайте той же датой, 2026-06-03, местами с расхождениями — в одной статье добавлен эпиграф). Оставшиеся 3 материала без веб-версии («Шагьикьа пачаясул полициялъул капитан», «ГӀункӀкӀги кетуги», «Кицаби, махсараби ва цоберал хабарал») добавлены в `hakikat.jsonl` напрямую из вычитанного DOCX с датой номера и ссылкой на оригинал вместо url. `data/ihdal.json` и запись `ihdal` в `sources.json` удалены; `books/ihdal.docx` остался — теперь как документ источника `hakikat`.

При сверке заголовок статьи `dur-tiagur-magiida-gioduleb-bugo-giin-kurun-ahiize-vachia-dir-emen` ошибочно поправили с «тӏамур» на «тӏагъур» — сочли расхождение с DOCX опечаткой сайта. Правка отменена: «тӏамур» (агач-комуз) в заголовке — авторская аллегория, на сайте hakikat.info было верно изначально.

## 2026-07-03 — удалены источники mukhidinov-library и murtazagaliev (вопросы правообладания)

По запросу правообладателей убраны два источника целиком: `mukhidinov-library` (Библиотека МухӀидиновых) и `murtazagaliev` (АхӀмад МуртазагӀалиев). Из репозитория удалены `data/mukhidinov-library.jsonl`, `data/murtazagaliev.jsonl`, все связанные документы из `books/` (DOCX/DOC/PDF), записи в `sources.json` и относящиеся к ним служебные md-файлы (`changelog/2026-07-01-mukhidinov-digit1-review.md`, `expert-review-murtazagaliev-digits.md`). Файлы сохранены вне репозитория; в git-истории они остаются.

## 2026-07-01 — av-ru: удалены битые see_also-ссылки на русские слова (8 статей)

`see_also.target` иногда указывал на русское слово-перевод, у которого нет отдельной аварской статьи — ссылка вела в никуда. Удалено: `дуниялалъул`→`война`, `карьеристлъи`→`карьерист`, `ревлюциялъул`→`революция`, `фотоувеличитель`→`увеличитель`, `гуталин`→`вакса` (второй see_also, `мазь`, оставлен — статья существует). Отдельно исправлена опечатка: `отоларингологлъи` ссылался на несуществующее `отоларинтолог`, должно быть `отоларинголог` (статья есть).

Уточнение: `казият`→`газета` и `канпит`→`конфета` не удалены, а исправлены на существующие аварские адаптации `газет` и `конфет` (без `-а`) — у `конфет` уже была обратная ссылка на `канпит`, теперь связь двусторонняя.

## 2026-07-01 — hakikat: OCR-гомоглифы латиница→кириллица и дефис перед аварскими суффиксами

Бренды (5 статей): `YouTubeканалалдасан` → `YouTube-каналалдасан`, `Telegramалда` → `Telegram-алда`, `Instagramалда` → `Instagram-алда`, `Groupалъул` → `Group-алъул`, `Zoomалдасан` → `Zoom-алдасан`.
OCR-гомоглифы в аварских диграфах (83 статьи): `xI`→`хӏ`, `XI`→`ХӀ`, `TI`→`тӏ` + прилегающие латинские `a`→`а`, `p`→`р`, `c`→`с`, `y`→`у`. Примеры: `xIapп`→`хӏарп`, `TIacaлъугьа`→`тӏасалъугьа`, `XIaп`→`ХӀап`, `axIyге`→`ахӏуге`, `Mapуса`→`Маруса`.

## 2026-07-01 — hakikat, phrase_av-ru: дефис перед аварским суффиксом у иноязычных аббревиатур

`hakikat.jsonl` (11 статей): `UFCялъул` → `UFC-ялъул`, `WADAялъул` → `WADA-ялъул`, `COVIDалъул` → `COVID-алъул`, `ITтехнологиязул` → `IT-технологиязул`, `VPNсервис` → `VPN-сервис`, `BMWялъув` → `BMW-ялъув`, `PFASалъул` → `PFAS-алъул`, `FDAялъул` → `FDA-ялъул`, `RAFалъул` → `RAF-алъул`, `AAOIFIялъул` → `AAOIFI-ялъул`.
`phrase_av-ru.jsonl` (1 статья): `чГораз реVЭдун` → `чӏораз реэдун` (OCR-гомоглифы).

## 2026-07-01 — murtazagaliev/tserehabi: удалены OCR-цифры (сноски) из 197 статей

Во всём сборнике `tserehabi` числа 1–9 прилипали к аварским словам (`кавуда4`, `Похъица5`, `гьобол5` и т. д.) — это метки сносок/стихов из исходного DOCX, слипшиеся при конвертации. Убраны все цифры непосредственно после кириллических символов. Затронуто 197 статей, удалено 202 цифры.

## 2026-07-01 — hakikat: удалены 3 статьи на не-аварских языках

Источник: `hakikat` (2632 → 2629 статей).

Удалены статьи из проекта газеты «Хӏакъикъат» по малым языкам Дагестана —
тексты не на аварском, а на цезском (дидойском) и гинухском языках:

- `6430-bezhkasal` — бежтинский фольклор (49x лӏ)
- `6606-ginuze` — гинухский фольклор (79x лӏ)
- `7326-tsezi-didojzas-folklor` — цезский фольклор (104x лӏ)

## 2026-07-01 — hakikat: исправлены 29 случаев палочки-как-цифры

Источник: `hakikat` (2632 статьи).

- `ХӏХ` → `ХIХ` (римская цифра XIX, «XIX век») — 21 замена в 17 статьях
- `Вӏ` → `В1` (витамин B1) — 4 замены в 2 статьях (3293, 4291)
- `ӏХ` → `IХ` (римская цифра IX) — 3 замены в 3 статьях (774, 7075, 12735)
- `Ӏ-себ` → `1-себ` (первый класс) — 1 замена в статье hӏakimchi-zhamgӏiyav-…

Источник: `hakikat` (2632 статьи).

- `Вӏ` → `В1` (витамин B1) — 4 замены в 2 статьях (3293, 4291)
- `ӏХ` → `IХ` (римская цифра IX) — 3 замены в 3 статьях (774, 7075, 12735)
- `Ӏ-себ` → `1-себ` (первый класс) — 1 замена в статье hӏakimchi-zhamgӏiyav-…

## 2026-06-30 — mukhidinov-library: контекстная нормализация палочки

Пересобран `data/mukhidinov-library.jsonl` после уточнения правил палочки
(CLAUDE.md, раздел «Аварская графика»):

- Палочка теперь стоит **только в диграфах** (после т/г/к/х/ц/ч).
- Регистр палочки совпадает с регистром основы (`ГӀумру`, не `Гӏумру`).
- Цифра `1` после диграф-основы → палочка (`т1ок1ав` → `тӏокӏав`).
- Палочка-глиф вне диграфа → цифра `1` (`Ӏ993` → `1993`, `Ӏ классалдаса` → `1 классалдаса`).
- Удвоение `чII`, `т11`, `тӏӏ` → одиночная палочка (`чӏ`, `тӏ`).

Суммарно: 123 724 палочки против 82 431 в первой версии (в основном за счёт книги
ГъалбацӀова, где типист печатал цифру `1` вместо палочки). Исправлены сотни лет/дат
(`1993`, `2014`, `11.08.43`, `24.01.94`), номеров классов и пр.

Также: `src/build_site.py::normalize_palochka` теперь следует тем же правилам — словари
(`av-ru`, `phrase-av-ru`) ребилдятся одинаково.

## 2026-06-30 — новый источник mukhidinov-library

Добавлен монолингвальный аварский корпус из шести современных книг, предоставленных
Шамилем и Баху МухӀидиновыми:

- Шамил МухӀидинов. ТӀохал… тӀохал… тӀохал… (2024) — литкритика/эссе.
- Шамил МухӀидинов. Сахаватаб гӀанхваро. Басняби (2022) — басни.
- Сборник докладов 2-го форума учителей авар. яз. (2017).
- ГъазимухӀамад ГъалбацӀов. Тирулеб сверулеб гьобоги буго… (2015) — публицистика.
- БакьагьечӀиса Даци. Мой ХӀукумат (2013) — стихи.
- ГъазимухӀамад ГъалбацӀов. Къалъелалде хъварал харбал — проза.

`data/mukhidinov-library.jsonl` — 6 записей, ~2,9 млн символов. Палочка приведена к каноническому
виду (U+04CF). Оригинальные DOCX/DOC уложены в `books/`. Парсер: `src/parse_mukhidinov_library.py`.

## 2026-06-26 — av-ru: метка (ласк.) у къочизе

Сверка с «Аварско-русский словарь 1.doc»: у примера «поцелуй меня, да умрёт твоя мать» метка
`(ласк.)` попала в `comment` (`ласкательная (форма)`) вместо `labels: ["ласкательная форма"]`.
Исправлена 1 статья (`къочизе`). Других чистых потерь `(ласк.)` в источнике нет; у `дада`/`дадал`
метка уже была на уровне значения или соседнего примера.

---

## 2026-06-29 — av-ru: тӏезе

Восстановлена структура статьи `тӏезе` (4 значения вместо 3 с перемешанными примерами):

- «сорвать / оборваться» — 8 примеров;
- «образоваться» (`comment`: о глубокой ране) — 1 пример;
- «лить / вливать / проливать» — 9 примеров;
- устойчивые выражения (`comment`, без `text`) — 4 идиомы.

1 статья.

## 2026-06-30 — ru-av: замена ӏ/Ӏ→1 внутри чисел (15 вхождений)

В `data/ru-av.jsonl` исправлены 15 случаев, где палочка стояла вместо цифры `1`:
годы (`ӏ792`, `ӏ657–ӏ838`, `ӏ838–ӏ9ӏ7`, `ӏ861`), числа (`ӏ0`, `Ӏ00`, `3ӏ`, `Ӏ2` и др.),
отдельные даты и количества. Два вхождения также имели потерянный пробел (исправлены).

## 2026-06-30 — hakikat: замена ӏ→1 внутри чисел (7 вхождений)

В `data/hakikat.jsonl` исправлены 7 случаев, где OCR принял цифру `1` за аварскую палочку `ӏ`:
`ӏ970` → `1970`, `202ӏ` → `2021`, `ӏ00` → `100` и т. д. Включая `covӏd`-подобные
конструкции. Смысловые переводы не менялись.

## 2026-06-29 — phrase-av-ru: дочистка 2 записей (раунд 4)

Последние 2 записи с `^` дочищены вручную по оригиналу:

- `ГӀУМРУЯЛДЕ БАХЪИНАБИЗЕ` (p. 107): `« ц ц ^` → `«цояв` в av; `призьш` → `призыв` в ru; `чш-л.` → `что-л.` в translation.
- `ЛЪИКӀ ГЬАЛУЛЕБ ХЬАГ` (p. 188): `Расу лицагицин` → `Расулицагицин` в av; `Рас>^ч` → `Расул` в ru.

Символов `^` в данных больше нет.

## 2026-06-29 — phrase-av-ru: исправление < > ^ и вьш* (раунд 3)

Обновлён `data/phrase_av-ru.jsonl` (2734 записи, без изменения числа).

- Исправлены 2 заголовка: `КВЕРЩЕЛ. ГЬАБИЗЕ` (точка) → `КВЕРЩЕЛ ГЬАБИЗЕ`; `СОРДО ЛЪИКП` → `СОРДО ЛЪИКӀ`.
- Убраны 10 символов `^` из полей `examples.av` и `examples.ru` (выпавшие буквы, OCR-замена `»`, шум).
- Исправлены 12 угловых скобок в переводах: `<` → `(`, `>` → `)` или убраны в конце строки.
- Исправлены ~15 слов с OCR-паттерном `вьш*`: `вьшолнить`→`выполнить`, `вьшасть`→`выпасть`,
  `вьшитое`→`вышитое`, `вьшосить`→`выносить`, `вьшрямились`→`выпрямились` и др.
- 2 поля `etymology` перенесены в текст `translation` (данные в скобках, не отдельное поле).

## 2026-06-29 — phrase-av-ru: исправление заголовков и OCR-шума (раунд 2)

Обновлён `data/phrase_av-ru.jsonl`: **2734** записи (+6 новых оборотов).

- Исправлены 26 заголовков: OCR-пробелы внутри слов убраны (`АЛ АГӀ А БОСИЗЕ` → `АЛАПА БОСИЗЕ` и т. д.).
- Добавлены 6 новых статей (страницы 15, 48, 112, 121, 124, 379, 381).
- Убраны 14 маркеров `^5:^` из переводов (заменены на `;`).
- 14 разделителей `\ ` заменены на `;`.
- Одиночный `^` убран из ~20 полей `literal`/`translation`.
- 4 метки (`погов.`, `рел.`, `груб.`) вынесены из текста перевода; 1 буквальный перевод перенесён в поле `literal`.
- В av-примерах убраны OCR-артефакты `-36` (номера страниц, склеившиеся со словом).

## 2026-06-29 — Новый источник: аварско-русский фразеологический словарь

Добавлен `data/phrase_av-ru.jsonl` — **2728** фразеологических единиц аварского языка
(источник: М. М. Магомедханов, «Аварско-русский фразеологический словарь», М.: Рус. яз.,
1993). Новый источник `phrase-av-ru` в `sources.json` (`kind: dictionary`, `status: test`).

- Схема записи: `phrase` (заглавный оборот), `variants`, `interjection`, `labels`
  (`посл.`, `погов.`, `книжн.` и т.д.), `senses[]` (`translation`, `literal`, `etymology`,
  `equiv`, `see_also`, `n`), `examples[]` (`av`, `ru`, `author`, `source`), `page`.
- Сборщик: введён хелпер `headword()` — словарный конвейер теперь берёт заголовок из
  `word` или `phrase`; подпись единиц параметризована полем `unit` источника.
- Оригинал: `books/Magomedkhanov_Avar-Russky_Frazeologichesky_Slovar.pdf`.
- Исправлены 48 дефектов извлечения: 40 статей, где текст примера затекал в `translation`/
  `literal` (пример разложён обратно на `av`/`ru`/`author`/`source`); 8 примеров с пустой
  половиной `av`/`ru`; 1 заголовок очищен от кавычек-ёлочек.
- Чистка OCR (185 строк): палочка в авторах `Г/.`/`Ц/.` → `ГӀ.`/`ЦӀ.` (125); склейка
  разорванных переносом русских слов в переводах и примерах `ru` (65).
- Восстановление палочки (556 статей): OCR-замены `П`/`Ш`/`/` → `ГӀ`/`Ӏ` в заголовках
  (241), аварском тексте примеров, авторах и источниках; 1 перевод очищен от мусорного
  префикса. Русские переводы по смыслу не менялись.
- Структурная вычитка (699 статей): исправлены имена авторов (`XI.`/`3.`/`Д/.` →
  `ХӀ.`/`ЦӀ.`, `Лбакар`→`Абакар`, `Хачалов`→`Качалов` и др.); восстановлены и уточнены
  названия произведений в `source`; буквальные переводы из ошибочных «примеров»
  (`author: "букв."`) перенесены в `senses[].literal`; из `author`/`source` удалены
  OCR-комментарии и сноски (18 полей), добавлено 29 полей `source`/`literal`. Чистое
  число примеров +4, смысловых правок переводов нет.
- Дочистка 3 перенесённых `literal` от OCR-мусора: `сЛШКИть`→`сложить`,
  `удов6лд>ствия`→`удовольствия`, `р6дом`→`родом`.
- Перекрёстные ссылки: добавлено поле `senses[].see_also_type` (`см.` / `то же, что`)
  ко всем 28 отсылкам; исправлены 2 обрезанных `see_also` (`ракӏ`→`ракӏ кьвагьизе`,
  `ункъабго`→`ункъабго къаникь лъезе`).

## 2026-06-26 — ru-av: поле stem и правки see_also

Обновлён `data/ru-av.jsonl`. Затронуто ~13 тыс. статей.

- Добавлено поле **`stem`** (основа для склонения): 6515 статей; у 3692 изменилось только `stem`.
- Правки **`see_also`**: 388 статей.
- При приёме исправлены регрессии парсера: палочка `Ⅰ`→`ӏ` (14 379 вхождений); пробелы
  внутри `stem` (1967); восстановлен заголовок `обязательный` (`word: null`).
- Подробно: [`changelog/2026-06-26-ru-av-stem.md`](changelog/2026-06-26-ru-av-stem.md).

## 2026-06-26 — ru-av: переразбор словаря

Обновлён `data/ru-av.jsonl` (переразбор из `ru-av.dict.txt`). Затронуто ~24 тыс. статей.

- Разнесение «русское слово → `text`, аварское толкование → `comment`» (заимствования и
  описательные толкования): ~2000 статей.
- Удалены дублирующие `forms` внутри значений (~5000 статей); добавлено поле `precomment`
  (русское пояснение перед толкованием, ~5000 статей); правки `see_also` (~1500).
- Нормализация палочки: глиф `Ⅰ` (U+2160) → `ӏ` (U+04CF) по всему файлу, 8020 вхождений
  (переразбор вносил ~6000 неверных глифов в `comment`; заодно вычищены и ранее существовавшие).
- Починен сломанный заголовок: статья с `word: null` (ранее мусорный `word: ","`) восстановлена
  как `обязательный`.
- Подробно: [`changelog/2026-06-26-ru-av-reparse.md`](changelog/2026-06-26-ru-av-reparse.md).

## 2026-06-24 — av-ru: восстановление вариантов значений (баг парсинга `а)/б)/в)`)

Исходный парсер `av-ru.dict.txt` терял подзначения идиом, оформленные как `а) … б) … в) …`:
первое значение оставалось с префиксом `а)`, а значения `б)/в)` пропадали (часто оседали
мусором в `sense.text`). Восстановлено по источнику `av-ru.dict.txt`.

- **97 статей** исправлено.
- Убран префикс `а) ` у первого значения; потерянные `б)/в)` значения восстановлены как
  отдельные примеры с тем же `av`.
- `sense.text` очищены от приклеенных фрагментов; метки (`перен.`, `погов.`, `рел.`, `бран.` …)
  и комментарии (`букв. …`, пояснения в скобках) разнесены по полям `labels`/`comment`.
- Сохранена конвенция датасета на необязательную приставку: `(по)валить`, `(с)делать`.
- **5 статей** пересобраны по структуре источника (ложные значения схлопнуты):
  `гъоркь` (нареч. + послелог), `тӏад` (нареч. + послелог), `кӏалдиб` (во рту + в пасти),
  `къокъ` (одно значение), `мегӏ` «вкус» (одно значение).

Подробный построчный diff по всем 97 статьям (для проверки корректорами):
[`changelog/2026-06-24-av-ru-variants.md`](changelog/2026-06-24-av-ru-variants.md).

## 2026-06-23 — av-ru: формы склонения местоимений а-серии

- Исправлены ошибочно редуплицированные формы склонения и примеры у указательных
  местоимений `аб/ав/ай/ал` с частицами (`-ги`, `-го`, `-али`, `-гияли`). 15 статей. (`d940e70`)
- Подробно: [`changelog/2026-06-23-av-ru-pronoun-declension.md`](changelog/2026-06-23-av-ru-pronoun-declension.md).

## 2026-06-19 — ru-av: правки

- Массив правок русско-аварского словаря, ~117 строк. (`d066c0c`)
- Подробно: [`changelog/2026-06-19-ru-av-fixes.md`](changelog/2026-06-19-ru-av-fixes.md).

## 2026-06-19 — av-ru: метки частей речи из текста значения

- Метки частей речи, попавшие в `sense.text`, вынесены в поля: `данде`, `гуреб`, `лъикӏ`,
  `-ниги`. 4 статьи. (`9ed1740`)
- Подробно: [`changelog/2026-06-19-av-ru-pos-in-sense-text.md`](changelog/2026-06-19-av-ru-pos-in-sense-text.md).

## 2026-06-18 — av-ru: кӏанцӏун вахъун ине

- Перевод «уйти» перенесён из поля `av` в `ru`. 1 статья. (`de60a0b`)
- Подробно: [`changelog/2026-06-18-av-ru-kantsun-vakhun-ine.md`](changelog/2026-06-18-av-ru-kantsun-vakhun-ine.md).

## 2026-06-17 — av-ru / ru-av: серия правок

- `сакъис`: косвенная форма `сакъисалдал` → `сакъисалъ`. (`418d554`)
- `сабру`: пример `кьуяги` → `кьеги`. (`85c8971`)
- Добавлено 13 новых статей (`саб`–`сали`). (`4000e2a`)
- `pos` `выражение` → `прилагательное` у 38 неверно классифицированных прилагательных. (`e5c859f`)
- `берцинаб`: `pos` `выражение` → `прилагательное`. (`963d5bb`)
- `берцинаб`: добавлена форма мн. ч. `берцинал` в `gender_forms`. (`90013d5`)
- Удвоенная `-о-` в косвенных формах: `гелдо`, `кӏертмахӏо`, `сумало`. (`580fd81`)
- Удвоенная `-о-` в косвенных формах `богӏоло`. (`00c7172`)
- Добавлены `gender_forms` к 1377 прилагательным на `-б`. (`0eb6873`)
- Обогащение статей `av-ru` полями `pos` и `form`. (`98f0105`)
- `ru-av`: обновлён парсинг. (`e81d384`)
- Подробно (полные diff мелких правок + сводки массовых):
  [`changelog/2026-06-17-series.md`](changelog/2026-06-17-series.md).

## 2026-06-17 — База

- Первичная публикация сайта и данных `sources.avar.me`. (`99a2a7a`)
