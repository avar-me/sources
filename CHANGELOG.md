# Changelog

Журнал изменений данных `data/*.jsonl` — источника правды экосистемы avar.me.

Фиксируем здесь **смысловые правки данных** (словарные статьи, формы, метки, корпусы),
а не служебные изменения сайта/шаблонов/CI. Потребители (`forms`, `avar.me`, `corrector`,
`index`) ориентируются по этому файлу, чтобы понимать, что и когда поменялось.

Формат: новые записи сверху, по датам. У каждой записи — дата, краткое описание, затронутый
источник (`av-ru`, `ru-av`, …) и объём правок. Коммит указывается, когда правка уже в `main`.

Крупные правки сопровождаются подробным построчным diff в папке [`changelog/`](changelog/)
(имя файла `ГГГГ-ММ-ДД-<источник>-<тема>.md`) — чтобы корректоры могли сверить каждую статью.

Исходные текстовые словари (`av-ru.dict.txt`, `ru-av.dict.txt`) в репозитории не храним; берём
их при необходимости из [avar-me/avar-russian-dictionary](https://github.com/avar-me/avar-russian-dictionary).

---

## 2026-06-29 — Новый источник: аварско-русский фразеологический словарь

Добавлен `data/phrase_av-ru.jsonl` — **2728** фразеологических единиц аварского языка
(источник: М. М. Магомедханов, «Аварско-русский фразеологический словарь», М.: Рус. яз.,
1993). Новый источник `phrase-av-ru` в `sources.json` (`kind: dictionary`, `status: test`).

- Схема записи: `phrase` (заглавный оборот), `variants`, `interjection`, `labels`
  (`посл.`, `погов.`, `книжн.` и т.д.), `senses[]` (`translation`, `literal`, `etymology`,
  `equiv`, `see_also`, `n`), `examples[]` (`av`, `ru`, `author`, `source`), `page`.
- Сборщик: введён хелпер `headword()` — словарный конвейер теперь берёт заголовок из
  `word` или `phrase`; подпись единиц параметризована полем `unit` источника.
- Оригинал: `books/Magomedkhanov_Avar-Russky_Frazeologichesky_Slovar.pdf`.
- Исправлены 48 дефектов извлечения: 40 статей, где текст примера затекал в `translation`/
  `literal` (пример разложён обратно на `av`/`ru`/`author`/`source`); 8 примеров с пустой
  половиной `av`/`ru`; 1 заголовок очищен от кавычек-ёлочек.
- Чистка OCR (185 строк): палочка в авторах `Г/.`/`Ц/.` → `ГӀ.`/`ЦӀ.` (125); склейка
  разорванных переносом русских слов в переводах и примерах `ru` (65).
- Восстановление палочки (556 статей): OCR-замены `П`/`Ш`/`/` → `ГӀ`/`Ӏ` в заголовках
  (241), аварском тексте примеров, авторах и источниках; 1 перевод очищен от мусорного
  префикса. Русские переводы по смыслу не менялись.

## 2026-06-26 — ru-av: поле stem и правки see_also

Обновлён `data/ru-av.jsonl`. Затронуто ~13 тыс. статей.

- Добавлено поле **`stem`** (основа для склонения): 6515 статей; у 3692 изменилось только `stem`.
- Правки **`see_also`**: 388 статей.
- При приёме исправлены регрессии парсера: палочка `Ⅰ`→`ӏ` (14 379 вхождений); пробелы
  внутри `stem` (1967); восстановлен заголовок `обязательный` (`word: null`).
- Подробно: [`changelog/2026-06-26-ru-av-stem.md`](changelog/2026-06-26-ru-av-stem.md).

## 2026-06-26 — ru-av: переразбор словаря

Обновлён `data/ru-av.jsonl` (переразбор из `ru-av.dict.txt`). Затронуто ~24 тыс. статей.

- Разнесение «русское слово → `text`, аварское толкование → `comment`» (заимствования и
  описательные толкования): ~2000 статей.
- Удалены дублирующие `forms` внутри значений (~5000 статей); добавлено поле `precomment`
  (русское пояснение перед толкованием, ~5000 статей); правки `see_also` (~1500).
- Нормализация палочки: глиф `Ⅰ` (U+2160) → `ӏ` (U+04CF) по всему файлу, 8020 вхождений
  (переразбор вносил ~6000 неверных глифов в `comment`; заодно вычищены и ранее существовавшие).
- Починен сломанный заголовок: статья с `word: null` (ранее мусорный `word: ","`) восстановлена
  как `обязательный`.
- Подробно: [`changelog/2026-06-26-ru-av-reparse.md`](changelog/2026-06-26-ru-av-reparse.md).

## 2026-06-24 — av-ru: восстановление вариантов значений (баг парсинга `а)/б)/в)`)

Исходный парсер `av-ru.dict.txt` терял подзначения идиом, оформленные как `а) … б) … в) …`:
первое значение оставалось с префиксом `а)`, а значения `б)/в)` пропадали (часто оседали
мусором в `sense.text`). Восстановлено по источнику `av-ru.dict.txt`.

- **97 статей** исправлено.
- Убран префикс `а) ` у первого значения; потерянные `б)/в)` значения восстановлены как
  отдельные примеры с тем же `av`.
- `sense.text` очищены от приклеенных фрагментов; метки (`перен.`, `погов.`, `рел.`, `бран.` …)
  и комментарии (`букв. …`, пояснения в скобках) разнесены по полям `labels`/`comment`.
- Сохранена конвенция датасета на необязательную приставку: `(по)валить`, `(с)делать`.
- **5 статей** пересобраны по структуре источника (ложные значения схлопнуты):
  `гъоркь` (нареч. + послелог), `тӏад` (нареч. + послелог), `кӏалдиб` (во рту + в пасти),
  `къокъ` (одно значение), `мегӏ` «вкус» (одно значение).

Подробный построчный diff по всем 97 статьям (для проверки корректорами):
[`changelog/2026-06-24-av-ru-variants.md`](changelog/2026-06-24-av-ru-variants.md).

## 2026-06-23 — av-ru: формы склонения местоимений а-серии

- Исправлены ошибочно редуплицированные формы склонения и примеры у указательных
  местоимений `аб/ав/ай/ал` с частицами (`-ги`, `-го`, `-али`, `-гияли`). 15 статей. (`d940e70`)
- Подробно: [`changelog/2026-06-23-av-ru-pronoun-declension.md`](changelog/2026-06-23-av-ru-pronoun-declension.md).

## 2026-06-19 — ru-av: правки

- Массив правок русско-аварского словаря, ~117 строк. (`d066c0c`)
- Подробно: [`changelog/2026-06-19-ru-av-fixes.md`](changelog/2026-06-19-ru-av-fixes.md).

## 2026-06-19 — av-ru: метки частей речи из текста значения

- Метки частей речи, попавшие в `sense.text`, вынесены в поля: `данде`, `гуреб`, `лъикӏ`,
  `-ниги`. 4 статьи. (`9ed1740`)
- Подробно: [`changelog/2026-06-19-av-ru-pos-in-sense-text.md`](changelog/2026-06-19-av-ru-pos-in-sense-text.md).

## 2026-06-18 — av-ru: кӏанцӏун вахъун ине

- Перевод «уйти» перенесён из поля `av` в `ru`. 1 статья. (`de60a0b`)
- Подробно: [`changelog/2026-06-18-av-ru-kantsun-vakhun-ine.md`](changelog/2026-06-18-av-ru-kantsun-vakhun-ine.md).

## 2026-06-17 — av-ru / ru-av: серия правок

- `сакъис`: косвенная форма `сакъисалдал` → `сакъисалъ`. (`418d554`)
- `сабру`: пример `кьуяги` → `кьеги`. (`85c8971`)
- Добавлено 13 новых статей (`саб`–`сали`). (`4000e2a`)
- `pos` `выражение` → `прилагательное` у 38 неверно классифицированных прилагательных. (`e5c859f`)
- `берцинаб`: `pos` `выражение` → `прилагательное`. (`963d5bb`)
- `берцинаб`: добавлена форма мн. ч. `берцинал` в `gender_forms`. (`90013d5`)
- Удвоенная `-о-` в косвенных формах: `гелдо`, `кӏертмахӏо`, `сумало`. (`580fd81`)
- Удвоенная `-о-` в косвенных формах `богӏоло`. (`00c7172`)
- Добавлены `gender_forms` к 1377 прилагательным на `-б`. (`0eb6873`)
- Обогащение статей `av-ru` полями `pos` и `form`. (`98f0105`)
- `ru-av`: обновлён парсинг. (`e81d384`)
- Подробно (полные diff мелких правок + сводки массовых):
  [`changelog/2026-06-17-series.md`](changelog/2026-06-17-series.md).

## 2026-06-17 — База

- Первичная публикация сайта и данных `sources.avar.me`. (`99a2a7a`)
