__pycache__/
*.pyc
.venv/
.env
.DS_Store
node_modules/

# Артефакты сборки — собираются Actions и публикуются на Pages, в репозиторий не коммитим
docs/

# Локальный pre-build кэш (если появится)
.cache/

# Рабочий срез открытых RFI из multilang.avar.me (см. CLAUDE.md → RFI) — не источник правды
issues.*.jsonl

# Генерируемые/рабочие PDF в корне (обзоры, канвасы, принесённые сканы) — не коммитим.
# Оригиналы словарей и книг лежат в books/ и отслеживаются.
/*.pdf

# Рабочее окружение и промежуточные артефакты разбора av-ru.1967 (см.
# av-ru-1967-parsing-handoff.md) — не коммитим.
.venv-av-ru-1967/
tmp/av-ru.1967/